---
phase: "02-native-registration-runtime"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-go/internal/registration/types.go
  - backend-go/internal/registration/http/handlers.go
  - backend-go/internal/registration/http/handlers_test.go
  - backend-go/internal/registration/http/integration_test.go
  - backend-go/internal/registration/batch_service.go
  - backend-go/internal/registration/batch_service_test.go
  - backend-go/internal/registration/outlook_service.go
  - backend-go/internal/registration/outlook_service_test.go
  - backend-go/internal/registration/ws/task_socket.go
  - backend-go/internal/registration/ws/task_socket_test.go
  - backend-go/internal/registration/ws/batch_socket.go
  - backend-go/internal/registration/ws/batch_socket_test.go
  - backend-go/internal/jobs/service.go
  - backend-go/internal/jobs/repository.go
  - backend-go/internal/jobs/repository_runtime.go
  - backend-go/internal/jobs/repository_memory.go
  - backend-go/tests/e2e/jobs_flow_test.go
autonomous: true
requirements:
  - RUN-02
  - COMP-03
user_setup: []
must_haves:
  truths:
    - "Existing clients can list, inspect, pause, resume, cancel, and clean up registration tasks and batches through Go with the current status and log semantics."
    - "Polling and websocket consumers observe compatible task, batch, and Outlook batch progress windows, including incremental offsets and cancelling transitions."
    - "The current frontend can reuse the Go registration/task/websocket surface without a contract rewrite."
  artifacts:
    - path: "backend-go/internal/registration/http/handlers.go"
      provides: "Python-compatible registration HTTP surface for single, batch, and Outlook batch controls."
    - path: "backend-go/internal/registration/batch_service.go"
      provides: "Durable batch projection with Python-compatible counters, log windows, and cancelling semantics."
    - path: "backend-go/internal/registration/ws/task_socket.go"
      provides: "Task websocket snapshot/control stream matching current frontend expectations."
    - path: "backend-go/internal/registration/ws/batch_socket.go"
      provides: "Batch websocket snapshot/control stream reused by standard and Outlook batch flows."
  key_links:
    - from: "backend-go/internal/registration/http/handlers.go"
      to: "backend-go/internal/registration/batch_service.go"
      via: "StartBatch/GetBatch/PauseBatch/ResumeBatch/CancelBatch"
      pattern: "StartBatch|GetBatch|PauseBatch|ResumeBatch|CancelBatch"
    - from: "backend-go/internal/registration/batch_service.go"
      to: "backend-go/internal/registration/ws/batch_socket.go"
      via: "BatchStatusResponse fields for status/log offsets/current_index/skipped"
      pattern: "status|log_base_index|log_next_offset|current_index|skipped"
---

<objective>
Complete the Go-side task, batch, Outlook batch, polling, and websocket runtime semantics that current clients already consume.

Purpose: Per D-01/D-03/D-05, close the observable compatibility gaps on top of the existing Go registration/jobs/websocket foundations, while keeping Phase 3 management APIs and Phase 4 payment/team work out of scope per D-06.
Output: Python-compatible HTTP and websocket semantics for registration start/task/batch/outlook control, status, and log windows.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-native-registration-runtime/02-CONTEXT.md
@.planning/phases/02-native-registration-runtime/02-RESEARCH.md
@.planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md
@.planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md
@src/web/routes/registration.py
@src/web/routes/websocket.py
@src/web/task_manager.py
@static/js/app.js
@backend-go/internal/registration/http/handlers.go
@backend-go/internal/registration/batch_service.go
@backend-go/internal/registration/outlook_service.go
@backend-go/internal/registration/ws/task_socket.go
@backend-go/internal/registration/ws/batch_socket.go
@backend-go/internal/jobs/service.go
@docs/superpowers/plans/2026-04-04-go-registration-batch-compat.md
@docs/superpowers/plans/2026-04-04-go-registration-outlook-batch-compat.md
@docs/superpowers/plans/2026-04-04-go-registration-frontend-contract.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 对齐 HTTP 轮询面与任务/批量清理语义</name>
  <files>backend-go/internal/registration/types.go, backend-go/internal/registration/http/handlers.go, backend-go/internal/registration/http/handlers_test.go, backend-go/internal/registration/http/integration_test.go, backend-go/internal/registration/batch_service.go, backend-go/internal/registration/batch_service_test.go, backend-go/internal/registration/outlook_service.go, backend-go/internal/registration/outlook_service_test.go, backend-go/internal/jobs/service.go, backend-go/internal/jobs/repository.go, backend-go/internal/jobs/repository_runtime.go, backend-go/internal/jobs/repository_memory.go, backend-go/tests/e2e/jobs_flow_test.go</files>
  <read_first>.planning/phases/02-native-registration-runtime/02-RESEARCH.md, .planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md, src/web/routes/registration.py, src/web/task_manager.py, static/js/app.js, backend-go/internal/registration/http/handlers.go, backend-go/internal/registration/batch_service.go, backend-go/internal/registration/outlook_service.go, backend-go/internal/jobs/service.go, backend-go/internal/jobs/repository.go</read_first>
  <behavior>
    - Test 1: `/api/registration/start`, `/batch`, and `/outlook-batch` return Python-compatible status codes and response shapes rather than accepted-only stubs.
    - Test 2: `/api/registration/tasks`, `/api/registration/tasks/{task_uuid}`, `/api/registration/tasks/{task_uuid}/logs`, and `DELETE /api/registration/tasks/{task_uuid}` support the list/inspect/cleanup contract current clients already use.
    - Test 3: batch and Outlook batch polling responses preserve `cancelling`, `current_index`, `skipped`, `log_base_index`, `log_offset`, and `log_next_offset` semantics expected by the current frontend.
  </behavior>
  <action>按 D-01/D-03/D-05 扩展当前 Go `registration` + `jobs` 兼容层，不要重建 Python 的 `task_manager`。在 `handlers.go`、`batch_service.go`、`outlook_service.go` 上补齐 Python-compatible HTTP contract：任务列表与删除、start/batch/outlook-batch 的 rich 200 响应、单任务和批量查询的状态字段、`cancelling` 中间态、`current_index`/`skipped`/`log_base_index`/全局 offset 语义。为支持 `/api/registration/tasks` 与删除能力，只在 `internal/jobs` 最小扩展当前服务/仓储查询与清理能力，不要引入 Phase 3 的 accounts/settings/logs 管理 API（D-06）。先在 `handlers_test.go`、`integration_test.go`、`batch_service_test.go`、`outlook_service_test.go`、`jobs_flow_test.go` 写失败用例，再以最小改动补实现。</action>
  <verify>cd backend-go && go test ./internal/registration/http ./internal/registration ./internal/jobs ./tests/e2e -run 'TestRegistrationCompatibleEndpoints|TestBatchEndpoints|TestOutlookBatchEndpoints|TestRegistrationStartAndTaskReadback|TestRegistrationCompatibilityFlow|TestRegistrationBatchCompatibilityFlow|TestRegistrationOutlookBatchCompatibility' -v</verify>
  <acceptance_criteria>
    - `backend-go/internal/registration/http/handlers.go` exposes `/api/registration/tasks` list and `DELETE /api/registration/tasks/{task_uuid}`
    - batch and Outlook batch polling responses include Python-compatible progress/log window fields rather than only minimal counters
    - targeted HTTP/integration/e2e tests prove current clients can inspect and clean up tasks through Go
  </acceptance_criteria>
  <done>Go HTTP polling surfaces cover the current task/batch/outlook runtime contract closely enough for existing clients to reuse them unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 对齐 task/batch websocket 帧形状与控制语义</name>
  <files>backend-go/internal/registration/ws/task_socket.go, backend-go/internal/registration/ws/task_socket_test.go, backend-go/internal/registration/ws/batch_socket.go, backend-go/internal/registration/ws/batch_socket_test.go, backend-go/tests/e2e/jobs_flow_test.go</files>
  <read_first>.planning/phases/02-native-registration-runtime/02-RESEARCH.md, src/web/routes/websocket.py, static/js/app.js, backend-go/internal/registration/ws/task_socket.go, backend-go/internal/registration/ws/task_socket_test.go, backend-go/internal/registration/ws/batch_socket.go, backend-go/internal/registration/ws/batch_socket_test.go</read_first>
  <behavior>
    - Test 1: task websocket sends the same initial status/log hydration contract current clients expect, then advances logs monotonically without replay drift.
    - Test 2: batch websocket control replies preserve `cancelling` visibility, compatible status payload shape, and heartbeat/ping behavior.
    - Test 3: Outlook batch continues to reuse `/api/ws/batch/{batch_id}` with the same control/log semantics as regular batch.
  </behavior>
  <action>按 D-03 只在现有 `ws/task_socket.go` 与 `ws/batch_socket.go` 基础上补齐帧形状和控制语义，不新建 websocket 路由族。保持 current frontend 已依赖的 snapshot-first、log streaming、ping/pong、pause/resume/cancel 协议，并对齐 research/sidecar 已确认的差异：batch status message shape、`timestamp`、`cancelling` 的 websocket 可见性、以及 Outlook batch 复用同一路由的兼容行为。优先通过 ws 单测和 `jobs_flow_test.go` 的 e2e 断言编码行为，再最小修正实现；不要把任何管理/payment/team websocket 迁移带入本 plan。</action>
  <verify>cd backend-go && go test ./internal/registration/ws ./tests/e2e -run 'TestTaskSocketSendsCurrentStatusAndLogs|TestTaskSocketCompletedStatusIncludesEmailFromJobResult|TestBatchSocketSendsCurrentStatusAndLogs|TestRegistrationWebSocketCompatibility|TestRegistrationBatchWebSocketCompatibility' -v</verify>
  <acceptance_criteria>
    - task websocket maintains monotonic log streaming and compatible status frames
    - batch websocket exposes `cancelling` and compatible control replies instead of collapsing directly to a terminal-only view
    - Outlook batch websocket consumption remains on `/api/ws/batch/{batch_id}` with matching semantics
  </acceptance_criteria>
  <done>Go websocket surfaces preserve the task and batch runtime semantics current polling/websocket consumers already use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client -> registration HTTP compatibility handlers | Untrusted control input crosses into task list, delete, pause/resume/cancel, and log offset handling here. |
| Browser client -> registration websocket handlers | Client control messages and reconnects can corrupt status/log sequencing if the compatibility contract drifts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | T | task/batch polling responses | mitigate | Preserve Python-compatible field names and offset semantics with HTTP/integration/e2e tests before changing implementation, especially `log_base_index`, `log_next_offset`, `current_index`, and `skipped`. |
| T-02-02-02 | D | websocket log sequencing | mitigate | Keep snapshot-first + monotonic incremental log behavior under unit and e2e coverage so reconnects do not duplicate or drop logs. |
| T-02-02-03 | R | pause/resume/cancel state transitions | mitigate | Encode `cancelling` intermediate visibility and control reply shape in handler/ws tests so control semantics remain traceable across HTTP and websocket channels. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd backend-go && go test ./internal/registration/http ./internal/registration ./internal/jobs ./tests/e2e -run 'TestRegistrationCompatibleEndpoints|TestBatchEndpoints|TestOutlookBatchEndpoints|TestRegistrationStartAndTaskReadback|TestRegistrationCompatibilityFlow|TestRegistrationBatchCompatibilityFlow|TestRegistrationOutlookBatchCompatibility' -v`
- [ ] `cd backend-go && go test ./internal/registration/ws ./tests/e2e -run 'TestTaskSocketSendsCurrentStatusAndLogs|TestTaskSocketCompletedStatusIncludesEmailFromJobResult|TestBatchSocketSendsCurrentStatusAndLogs|TestRegistrationWebSocketCompatibility|TestRegistrationBatchWebSocketCompatibility' -v`
- [ ] Current Go handlers/websockets expose no new Phase 3 or Phase 4 API surface
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Existing clients can list, inspect, control, and clean up tasks/batches through Go with compatible semantics
- Polling and websocket consumers observe compatible progress/log windows without frontend rewrites
- No management/payment/team migration work was pulled into the plan
</success_criteria>

<output>
After completion, create `.planning/phases/02-native-registration-runtime/02-02-SUMMARY.md`
</output>
