---
phase: 04-payment-and-team-domains
plan: "07"
type: execute
wave: 3
depends_on: ["04-04", "04-06"]
files_modified:
  - backend-go/internal/payment/transition_adapters.go
  - backend-go/internal/payment/transition_adapters_test.go
  - backend-go/cmd/api/payment_runtime_test.go
autonomous: true
gap_closure: true
requirements: [PAY-01]
must_haves:
  truths:
    - "Payment transition bootstrap must not persist fabricated session tokens into the shared accounts truth source."
    - "Transition browser-open semantics must remain truthful: fallback/no-browser behavior may be compatible, but the API must not claim a browser launched when no launch occurred."
    - "Bootstrap-level tests prove the payment runtime helper preserves these truthfulness rules under the current constructor path."
  artifacts:
    - "backend-go/internal/payment/transition_adapters.go only returns bootstrap success when a real reusable session token/cookie exists."
    - "backend-go/internal/payment/transition_adapters.go no longer reports `OpenIncognito` success for arbitrary non-empty URLs."
    - "backend-go/internal/payment/transition_adapters_test.go and backend-go/cmd/api/payment_runtime_test.go lock the no-synthetic-token and truthful-browser-open behavior."
  key_links:
    - "`BootstrapAccountSessionToken(...)` must not write placeholder `session_token` data through accounts persistence."
    - "Payment helper tests must fail if browser-open or session-bootstrap regress back to false-success behavior."
---

<objective>
Close the payment truthfulness gaps introduced by the Phase 4 transition adapters without reopening route ownership or Phase 5 cutover work.

Purpose: 04-04 removed hollow constructor failures, but code review found two remaining correctness issues inside the transition adapter boundary: synthetic session bootstrap writeback and false-success browser opening. This plan fixes those semantics while preserving the Go-owned adapter seam.
Output: corrected payment transition adapter behavior plus focused regression tests.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-payment-and-team-domains/04-04-PLAN.md
@.planning/phases/04-payment-and-team-domains/04-04-SUMMARY.md
@.planning/phases/04-payment-and-team-domains/04-06-SUMMARY.md
@backend-go/internal/payment/transition_adapters.go
@backend-go/internal/payment/transition_adapters_test.go
@backend-go/cmd/api/payment_runtime.go
@backend-go/cmd/api/payment_runtime_test.go
@backend-go/internal/payment/service.go
@src/web/routes/payment.py

<review_findings>
- Review finding A: `BootstrapSessionToken` fabricates `session-bootstrap-{id}` when there is no real session token, then `BootstrapAccountSessionToken` persists that placeholder into `accounts`.
- Review finding B: `OpenIncognito` returns success for any non-empty URL, which causes `/open` and auto-open paths to claim the browser launched even when nothing happened.
</review_findings>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove synthetic session bootstrap writeback and preserve truthful bootstrap semantics</name>
  <files>backend-go/internal/payment/transition_adapters.go, backend-go/internal/payment/transition_adapters_test.go, backend-go/cmd/api/payment_runtime_test.go</files>
  <behavior>
    - Test 1: bootstrap succeeds only when an existing reusable session token is already present in account/session cookies; access token or refresh token alone must not fabricate a new `session_token`.
    - Test 2: helper-built payment service must not persist a placeholder token into the accounts truth source when no real session token exists.
    - Test 3: existing session-diagnostic / subscription-sync compatibility behavior stays intact.
  </behavior>
  <action>Replace the current synthetic session bootstrap fallback with truthful transition behavior. Reuse existing session token/cookie parsing only; if no reusable session token exists yet, return the compatible “not bootstrapped” shape instead of inventing one. Keep the change inside the payment adapter boundary and update targeted tests to prove accounts persistence no longer receives fabricated `session_token` values.</action>
  <verify>
    <automated>cd backend-go && go test ./internal/payment -run 'Test(PaymentTransitionAdapter|PaymentSubscription).*' -v && go test ./cmd/api -run 'TestAPIPaymentRuntime.*' -v</automated>
  </verify>
  <done>Payment bootstrap no longer pollutes shared account state with fabricated session tokens, and the current helper/runtime tests lock that behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore truthful browser-open fallback semantics without reintroducing nil-seam regressions</name>
  <files>backend-go/internal/payment/transition_adapters.go, backend-go/internal/payment/transition_adapters_test.go, backend-go/cmd/api/payment_runtime_test.go</files>
  <behavior>
    - Test 1: transition browser-open must not report success unless an actual launch path is implemented.
    - Test 2: payment helper/runtime tests must prove `/open`-related behavior no longer returns false-positive success.
    - Test 3: the fix must preserve Phase 4 payment smoke coverage without adding Phase 5 browser automation scope.
  </behavior>
  <action>Make the transition browser opener truthful again. It may keep a compatible manual-fallback path, but it must not claim “opened” or push bind-card tasks into `opened` solely because a URL exists. Update tests so they reflect truthful fallback semantics instead of fake-open success, while keeping payment helper coverage focused on seam completeness rather than pretending a browser launched.</action>
  <verify>
    <automated>cd backend-go && go test ./internal/payment -run 'TestPaymentTransitionAdapter.*' -v && go test ./cmd/api -run 'TestAPIPaymentRuntime.*' -v</automated>
  </verify>
  <done>Transition browser-open behavior is truthful, and payment runtime coverage no longer relies on fake-open success semantics.</done>
</task>

</tasks>

<verification>
- `cd backend-go && go test ./internal/payment -run 'Test(PaymentTransitionAdapter|PaymentSubscription).*' -v`
- `cd backend-go && go test ./cmd/api -run 'TestAPIPaymentRuntime.*' -v`
</verification>

<success_criteria>
- Payment transition bootstrap writes only real reusable session data.
- Payment transition browser-open no longer reports false success.
- No new Phase 5 browser/cutover work is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/04-payment-and-team-domains/04-07-SUMMARY.md`
</output>
