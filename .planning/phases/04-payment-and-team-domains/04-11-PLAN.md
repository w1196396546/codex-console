---
phase: 04-payment-and-team-domains
plan: "11"
type: execute
wave: 5
depends_on: ["04-10"]
files_modified:
  - backend-go/internal/team/tasks.go
  - backend-go/internal/team/tasks_test.go
  - backend-go/internal/team/transition_gateway.go
  - backend-go/internal/team/transition_executor.go
  - backend-go/internal/team/transition_executor_test.go
autonomous: true
gap_closure: true
requirements: [TEAM-01]
must_haves:
  truths:
    - "Accepted Team tasks must not hang forever when upstream transition HTTP calls stall."
    - "If task persistence fails after job creation, the runtime must not leak orphan jobs."
    - "Regression tests fail if Team transition runtime blocks indefinitely or leaves orphaned jobs on admission failure."
  artifacts:
    - "backend-go/internal/team/transition_gateway.go and transition_executor.go apply a bounded timeout to transition HTTP requests when no tighter caller deadline exists."
    - "backend-go/internal/team/tasks.go cleans up or marks failed any just-created job when `CreateTask` cannot persist the accepted Team task."
    - "backend-go/internal/team/tasks_test.go and transition_executor_test.go cover timeout and orphan-job cleanup behavior."
  key_links:
    - "`launchAcceptedTask()` must not rely on an unbounded `context.Background()` path for upstream Team I/O."
    - "`createAcceptedTask()` must leave jobs/runtime storage consistent with repository admission outcomes."
---

<objective>
Close the remaining Team runtime resilience gaps: unbounded upstream hangs and orphan job leakage on admission failure.

Purpose: Final code review found that accepted Team tasks can hang forever because transition HTTP calls use no timeout, and `createAcceptedTask()` can leave orphan jobs if persistence fails after `CreateJob()`.
Output: bounded Team transition runtime plus admission cleanup guarantees and regression tests.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend-go/internal/team/tasks.go
@backend-go/internal/team/tasks_test.go
@backend-go/internal/team/transition_gateway.go
@backend-go/internal/team/transition_executor.go
@backend-go/internal/jobs/service.go
@backend-go/internal/jobs/repository_memory.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bound Team transition HTTP/runtime execution with a deterministic timeout</name>
  <files>backend-go/internal/team/transition_gateway.go, backend-go/internal/team/transition_executor.go, backend-go/internal/team/transition_executor_test.go</files>
  <behavior>
    - Test 1: transition HTTP calls honor caller deadlines and also apply a sensible default timeout when called from background task execution.
    - Test 2: a stalled upstream transport surfaces a failed task/result instead of hanging forever.
  </behavior>
  <action>Add a bounded timeout around Team transition HTTP calls when the incoming context has no deadline. Keep the timeout localized to the transition HTTP boundary so normal fast-path semantics do not change, but accepted tasks can no longer hang forever on a wedged upstream request.</action>
  <verify>
    <automated>cd backend-go && go test ./internal/team -run 'Test(TeamTransitionExecutor|TeamTransitionGateway).*' -v</automated>
  </verify>
  <done>Team transition runtime no longer hangs indefinitely on stalled upstream requests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clean up runtime jobs when accepted-task persistence fails after `CreateJob`</name>
  <files>backend-go/internal/team/tasks.go, backend-go/internal/team/tasks_test.go</files>
  <behavior>
    - Test 1: if `CreateTask` fails after `CreateJob` succeeds, the newly created job is deleted or otherwise reconciled so no orphan pending job remains.
    - Test 2: normal accepted-task creation keeps current websocket/task contract unchanged.
  </behavior>
  <action>Introduce minimal cleanup logic in `createAcceptedTask()` so a failed repository admission does not leak a job into runtime storage without a corresponding `team_tasks` row. Reuse existing jobs control primitives where available; do not add Phase 5 queue redesign work.</action>
  <verify>
    <automated>cd backend-go && go test ./internal/team -run 'Test(Accepted|TeamTaskLiveExecution|DiscoveryAcceptedTaskReusesOwnerScopeAndWsChannel).*' -v</automated>
  </verify>
  <done>Accepted Team task admission leaves jobs/runtime and repository state consistent even on failure.</done>
</task>

</tasks>

<verification>
- `cd backend-go && go test ./internal/team -run 'Test(TeamTransitionExecutor|TeamTransitionGateway).*' -v`
- `cd backend-go && go test ./internal/team -run 'Test(Accepted|TeamTaskLiveExecution|DiscoveryAcceptedTaskReusesOwnerScopeAndWsChannel).*' -v`
</verification>

<success_criteria>
- Team transition runtime has bounded failure behavior under stalled upstream calls.
- Accepted Team task admission no longer leaks orphan jobs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-payment-and-team-domains/04-11-SUMMARY.md`
</output>
