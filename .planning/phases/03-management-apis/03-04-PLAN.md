---
phase: 03-management-apis
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-go/internal/uploader/types.go
  - backend-go/internal/uploader/repository_postgres.go
  - backend-go/internal/uploader/service.go
  - backend-go/internal/uploader/http/handlers.go
  - backend-go/internal/uploader/service_test.go
  - backend-go/internal/uploader/http/handlers_test.go
autonomous: true
requirements: [MGMT-02]
must_haves:
  truths:
    - "现有 settings 页面和 accounts 页面依赖的 `/api/cpa-services*`、`/api/sub2api-services*`、`/api/tm-services*` 路由都由 Go 提供。"
    - "CPA/Sub2API/TM 服务配置 CRUD、`/full`、`test-connection`、`/test` 与 Sub2API 直传动作保持当前字段和协议语义。"
  artifacts:
    - "backend-go/internal/uploader/service.go 承担 upload config admin orchestration。"
    - "backend-go/internal/uploader/http/handlers.go 暴露三个 upload-service 路由族。"
  key_links:
    - "配置管理与实际外发共用同一个 `uploader` package，不复制 payload builder/sender。"
    - "Sub2API 的 `target_type`、CPA 的 token 可见性、TM 的 API key 语义都延续当前表结构。"
---

<objective>
完成上传服务配置管理 API 的 Go 迁移，并保持账户页/设置页对 CPA、Sub2API、TM 配置和直传动作的现有契约。

Purpose: 落地 `MGMT-02` 中 upload-config 子域，严格复用既有 `uploader` foundations。
Output: uploader admin service/handlers、CRUD/full/test/upload 兼容端点、配套单元与 handler 测试。
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-management-apis/03-CONTEXT.md
@.planning/phases/03-management-apis/03-RESEARCH.md
@src/web/routes/upload/cpa_services.py
@src/web/routes/upload/sub2api_services.py
@src/web/routes/upload/tm_services.py
@static/js/settings.js
@static/js/accounts.js
@backend-go/internal/uploader/types.go
@backend-go/internal/uploader/repository_postgres.go
@backend-go/internal/uploader/builder.go
@backend-go/internal/uploader/sender.go

<interfaces>
Current Go uploader contracts:
```go
type ConfigRepository interface {
	ListCPAServiceConfigs(ctx context.Context, ids []int) ([]ServiceConfig, error)
	ListSub2APIServiceConfigs(ctx context.Context, ids []int) ([]ServiceConfig, error)
	ListTMServiceConfigs(ctx context.Context, ids []int) ([]ServiceConfig, error)
}

func NewSender(kind UploadKind, doer HTTPDoer) (Sender, error)
```

Python compatibility surface:
```text
GET/POST/PATCH/DELETE /api/cpa-services*
GET/POST/PATCH/DELETE /api/sub2api-services*
GET/POST/PATCH/DELETE /api/tm-services*
GET /api/{cpa-services|sub2api-services}/{service_id}/full
POST /api/{cpa-services|sub2api-services|tm-services}/{service_id}/test
POST /api/{cpa-services|sub2api-services|tm-services}/test-connection
POST /api/sub2api-services/upload
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 uploader package 内补齐配置管理 contracts、repository 写侧与 service</name>
  <files>backend-go/internal/uploader/types.go, backend-go/internal/uploader/repository_postgres.go, backend-go/internal/uploader/service.go, backend-go/internal/uploader/service_test.go</files>
  <behavior>
    - Test 1: CPA/Sub2API/TM 配置的 list/get/full/create/update/delete 保持当前字段差异和数组/envelope 语义。
    - Test 2: `sub2api.target_type`、`priority`、`enabled`、token/key 可见性规则与当前 Python 端一致。
  </behavior>
  <action>按 D-06/D-07 直接扩展现有 `uploader` package，不新建平行 upload-admin package。repository 写侧和 admin service 需要覆盖三类服务的 CRUD/full/list 读取，但必须继续复用现有 `ServiceConfig`、payload builder、sender 和表结构语义。不要为任何目标系统重新发明 payload 或 HTTP client。</action>
  <verify>
    <automated>cd backend-go && go test ./internal/uploader -run 'Test(Upload|ServiceConfig|Builder|Sender).*' -v</automated>
  </verify>
  <done>uploader package 自身已经能表达 CPA/Sub2API/TM 配置管理读写合同，不需要 Python CRUD 作为中间层。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 暴露 upload-config 兼容 handlers，并保留 Sub2API 直传/测试动作</name>
  <files>backend-go/internal/uploader/http/handlers.go, backend-go/internal/uploader/http/handlers_test.go, backend-go/internal/uploader/service.go, backend-go/internal/uploader/service_test.go</files>
  <behavior>
    - Test 1: 三类 `/api/*-services` 路由保持当前路径、方法、列表结构、`/full` 行为和测试连接结果。
    - Test 2: `/api/sub2api-services/upload` 与 `test-connection`/`/{id}/test` 动作继续复用现有 sender/builder，不出现第二套协议实现。
  </behavior>
  <action>把 CPA/Sub2API/TM 的 CRUD/full/test/test-connection/Sub2API direct upload 路由全部落到 `backend-go/internal/uploader/http`。accounts 页的手动上传端点不在这里实现；本计划只完成 upload-config admin surface。确保 list 端点维持 Python 当前的 plain-array 返回风格，避免统一改造成新的对象 envelope。</action>
  <verify>
    <automated>cd backend-go && go test ./internal/uploader -run 'Test(Upload|Handler|Sender|Builder).*' -v</automated>
  </verify>
  <done>settings/accounts 页依赖的 upload-config 管理接口已有 Go owner，且仍复用既有 uploader foundations。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→`/api/*-services` | 非可信服务配置和测试请求进入上传配置管理端点 |
| uploader admin→external target systems | `test`、`test-connection` 和直传会触达外部目标服务 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | T | upload-config create/update handlers | mitigate | 所有配置变更使用 typed request structs，并保留当前 `enabled/priority/target_type` 范围校验，防止将无效配置写入共享表。 |
| T-03-04-02 | I | list/full 端点 | mitigate | 锁定 list 与 `/full` 的敏感字段差异：list 只暴露 `has_token/has_key` 等兼容字段，`/full` 才返回密钥。 |
| T-03-04-03 | D | 外部 `test`/`upload` 动作 | mitigate | 统一经现有 `uploader` sender/client 执行，沿用超时、响应解析和消息归一化逻辑，禁止在 handler 直接拼 HTTP 请求。 |
</threat_model>

<verification>
- `cd backend-go && go test ./internal/uploader -run 'Test(Upload|ServiceConfig|Builder|Sender).*' -v`
- `cd backend-go && go test ./internal/uploader -run 'Test.*Handler.*' -v`
</verification>

<success_criteria>
- 三类 upload-config 管理路由和 Sub2API 直传动作都由 Go 提供，且当前页面无需改 JS。
- 所有外部上传协议继续复用现有 `uploader` foundations，不产生第二套实现。
</success_criteria>

<output>
After completion, create `.planning/phases/03-management-apis/03-management-apis-04-SUMMARY.md`
</output>
