---
phase: 04-payment-and-team-domains
plan: "05"
type: execute
wave: 1
depends_on: ["04-02"]
files_modified:
  - backend-go/internal/team/transition_gateway.go
  - backend-go/internal/team/transition_gateway_test.go
  - backend-go/internal/team/transition_executor.go
  - backend-go/internal/team/transition_executor_test.go
  - backend-go/internal/team/tasks.go
  - backend-go/internal/team/tasks_test.go
  - backend-go/cmd/api/team_runtime.go
  - backend-go/cmd/api/team_runtime_test.go
autonomous: true
gap_closure: true
requirements: [TEAM-01]
must_haves:
  truths:
    - "The live team service has a concrete `MembershipGateway`, so revoke/remove no longer fail with `membership gateway not configured`."
    - "Accepted discovery, sync, and invite tasks have a Go-owned execution launch path and progress beyond `pending` under the live constructor path instead of requiring a manual `ExecuteTask(...)` call from tests."
    - "Bootstrap-level tests prove the current team constructor wiring and execution chain, not only isolated fake-seam behavior."
  artifacts:
    - "backend-go/internal/team/transition_gateway.go provides the live membership gateway used by revoke/remove."
    - "backend-go/internal/team/transition_executor.go plus the updated `backend-go/internal/team/tasks.go` provide a concrete executor and accepted-task launch path for discovery/sync/invite."
    - "backend-go/cmd/api/team_runtime.go builds fully-wired team services, and `backend-go/cmd/api/team_runtime_test.go` proves revoke/remove plus accepted-task execution under current constructors."
  key_links:
    - "`backend-go/cmd/api/team_runtime.go` injects both `MembershipGateway` and `TaskExecutor` into `team.NewService(...)` and `team.NewTaskService(...)`."
    - "`backend-go/internal/team/tasks.go` launches accepted tasks through the shared jobs/task-websocket control plane without reintroducing Python `task_manager` or a Team-only websocket."
---

<objective>
Close the hollow team bootstrap by supplying live membership and task-execution runtime pieces, then verify that accepted Team work actually runs through the Go-owned chain.

Purpose: The team slice and routes already exist, but the live constructor still injects nil gateway/executor values and leaves accepted work without execution. This plan closes that exact gap and nothing beyond Phase 4.
Output: Go-owned team transition gateway/executor, an accepted-task launch path, an API runtime helper, and constructor-level tests that guard against regressions.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-payment-and-team-domains/04-VERIFICATION.md
@.planning/phases/04-payment-and-team-domains/04-02-SUMMARY.md
@.planning/phases/04-payment-and-team-domains/04-03-SUMMARY.md
@backend-go/cmd/api/main.go
@backend-go/internal/team/service.go
@backend-go/internal/team/tasks.go
@backend-go/internal/team/types.go
@backend-go/tests/e2e/payment_team_flow_test.go
@src/services/team/membership_actions.py
@src/services/team/runner.py
@src/services/team/discovery.py
@src/services/team/sync.py
@src/services/team/invite.py
@src/services/team/client.py

<interfaces>
Current live bootstrap leaves Team hollow:
```go
teamService := team.NewService(teamRepository, nil)
teamTaskService := team.NewTaskService(teamRepository, teamService, jobService, nil)
```

Current team seams that must be concretely wired:
```go
type MembershipGateway interface {
    RevokeInvite(ctx context.Context, params MembershipGatewayRevokeInviteParams) error
    RemoveMember(ctx context.Context, params MembershipGatewayRemoveMemberParams) error
}

type TaskExecutor interface {
    Execute(ctx context.Context, task TaskExecutionRequest) (TaskExecutionResult, error)
}
```

Current e2e still has to manually push execution:
```go
go func() {
    done <- taskService.ExecuteTask(context.Background(), taskUUID)
}()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the live Team membership gateway, executor, and accepted-task launch path</name>
  <files>backend-go/internal/team/transition_gateway.go, backend-go/internal/team/transition_gateway_test.go, backend-go/internal/team/transition_executor.go, backend-go/internal/team/transition_executor_test.go, backend-go/internal/team/tasks.go, backend-go/internal/team/tasks_test.go</files>
  <behavior>
    - Test 1: revoke/remove uses a concrete `MembershipGateway` that preserves the current success, detail, and status semantics from `src/services/team/membership_actions.py` instead of returning `membership gateway not configured`.
    - Test 2: accepted discovery, sync, and invite tasks trigger a Go-owned launch path after admission and progress pending→running→completed/failed without a manual `ExecuteTask(...)` call outside the runtime path.
    - Test 3: the accepted-task launch path continues to reuse `jobs.Service` and `/api/ws/task/{task_uuid}`, keeps `active_scope_key` dedupe intact, and closes the previously deferred invite execution gap.
  </behavior>
  <action>Per D-01, D-03, D-04, D-06, and D-07, add a concrete Team transition runtime inside `backend-go/internal/team`. `transition_gateway.go` should encapsulate the current upstream revoke/remove behavior from `src/services/team/membership_actions.py` and `src/services/team/client.py`. `transition_executor.go` should encapsulate the current discovery/sync/invite execution semantics from `src/services/team/runner.py`, `discovery.py`, `sync.py`, and `invite.py`. Update `backend-go/internal/team/tasks.go` so accepted tasks are launched through a controlled Go-owned path immediately after admission or through an explicit injected launcher, but never by reviving Python `task_manager`, adding a Team-only websocket, or leaving invite work as accepted-only again.</action>
  <verify>
    <automated>cd backend-go && go test ./internal/team -run 'Test(TeamTransitionGateway|TeamTaskLiveExecution|Accepted|Invite|Discovery|Sync).*' -v</automated>
  </verify>
  <done>The Team slice has a live membership gateway and task executor, and accepted discovery/sync/invite work can advance beyond `pending` under Go-owned runtime control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an API bootstrap helper and constructor-level Team wiring tests</name>
  <files>backend-go/cmd/api/team_runtime.go, backend-go/cmd/api/team_runtime_test.go</files>
  <behavior>
    - Test 1: a helper-built Team service/task-service pair injects non-nil `MembershipGateway` and `TaskExecutor`.
    - Test 2: helper-driven revoke/remove no longer returns `membership gateway not configured`, and helper-driven accepted tasks move beyond `pending` without out-of-band manual execution in the test body.
  </behavior>
  <action>Create `backend-go/cmd/api/team_runtime.go` with a helper such as `newAPITeamServices(...)` that composes the Team repository, read service, membership gateway, task executor, and accepted-task launch configuration from Task 1. Add `backend-go/cmd/api/team_runtime_test.go` to prove the current constructor path is operational. These tests must use the helper-built services and the shared jobs/task websocket model, because the blocker to close is that `cmd/api` currently wires nil dependencies even though the slice exists.</action>
  <verify>
    <automated>cd backend-go && go test ./cmd/api -run 'TestAPITeamRuntime.*' -v</automated>
  </verify>
  <done>The API package exposes fully-wired Team services, and bootstrap-level tests guard against the gateway/executor being omitted again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/static-js→team service/task service | Existing Team pages send membership actions and accepted-task requests into the Go Team slice. |
| team transition runtime→upstream Team API | Revoke/remove/discovery/sync/invite behavior may still depend on upstream Team API calls or bounded transition logic. |
| team task launch path→shared jobs/websocket runtime | Accepted Team work must reuse the shared task UUID and websocket projection instead of diverging into a second control plane. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | T | membership gateway | mitigate | Keep revoke/remove upstream calls in one `transition_gateway.go` implementation and add tests that lock current success/error semantics and state transitions. |
| T-04-05-02 | D | accepted-task execution chain | mitigate | Extend `tasks.go` with a tested launch path so accepted tasks cannot stall forever at `pending` when the live constructor is used. |
| T-04-05-03 | R | Team task observability | mitigate | Preserve shared `task_uuid`/`ws_channel` semantics and test that accepted work still reaches the existing `/api/ws/task/{task_uuid}` projection. |
</threat_model>

<verification>
- `cd backend-go && go test ./internal/team -run 'Test(TeamTransitionGateway|TeamTaskLiveExecution|Accepted|Invite|Discovery|Sync).*' -v`
- `cd backend-go && go test ./cmd/api -run 'TestAPITeamRuntime.*' -v`
</verification>

<success_criteria>
- Team revoke/remove and accepted discovery/sync/invite work through concrete live dependencies instead of nil seams.
- The Team slice keeps the shared jobs/websocket runtime model already established in Phase 2.
- No Phase 5 cutover, rollout, or new websocket protocol is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/04-payment-and-team-domains/04-05-SUMMARY.md`
</output>
