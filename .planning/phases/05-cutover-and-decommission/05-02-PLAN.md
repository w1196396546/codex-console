---
phase: 05-cutover-and-decommission
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docker-compose.yml
  - scripts/docker/start-webui.sh
  - webui.py
  - src/web/app.py
  - backend-go/internal/registration/python_runner.go
  - backend-go/internal/registration/python_runner_script.go
  - src/web/routes/payment.py
  - README.md
  - backend-go/README.md
  - .planning/phases/05-cutover-and-decommission/05-VERIFICATION.md
autonomous: true
requirements: [CUT-02]
must_haves:
  truths:
    - "Python backend responsibilities must be removed from the production critical path or isolated behind an explicit, non-critical compatibility boundary."
    - "Production startup assets must no longer default to Python-owned backend behavior after cutover."
    - "Residual Python bridges and helpers must have a clear final disposition: removed, isolated, or explicitly accepted as non-critical legacy."
  artifacts:
    - "Runtime startup/config assets reflect the Go-owned backend path as the supported production baseline."
    - "Residual Python bridge responsibilities are inventoried and documented with disposition and rollback implications."
    - "Final Phase 5 verification records whether any Python shell remains and why it is not backend-critical."
  key_links:
    - "The final deployment surface must match the topology defined in `05-01`."
    - "Any retained Python code must be demonstrably outside the production backend critical path."
---

<objective>
Remove or isolate the remaining Python backend responsibilities from the production path after the Go cutover topology and verification gate exist. This plan finishes the migration by making the deployment and runtime ownership match the already-migrated domain contracts.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cutover-and-decommission/05-CONTEXT.md
@.planning/phases/05-cutover-and-decommission/05-RESEARCH.md
@.planning/phases/05-cutover-and-decommission/05-01-PLAN.md
@.planning/phases/04-payment-and-team-domains/04-VERIFICATION.md
@webui.py
@src/web/app.py
@src/web/routes/payment.py
@scripts/docker/start-webui.sh
@docker-compose.yml
@backend-go/cmd/api/main.go
@backend-go/cmd/worker/main.go
@backend-go/internal/registration/python_runner.go
@backend-go/internal/registration/python_runner_script.go
@README.md
@backend-go/README.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewire production startup assets to reflect the Go-owned backend path</name>
  <files>docker-compose.yml, scripts/docker/start-webui.sh, README.md, backend-go/README.md</files>
  <behavior>
    - Production-oriented startup assets stop defaulting to Python-owned backend behavior.
    - If a Python shell remains, its role is explicit and non-critical.
    - Rollback instructions from `05-01` remain valid after the startup-asset changes.
  </behavior>
  <action>Update the deployment surface so the supported production baseline matches the Go-owned topology defined in `05-01`. Do not silently keep Python as the hidden backend path after cutover.</action>
  <verify>
    <automated>rg -n "webui.py|run-api|run-worker|docker-compose|production|rollback" README.md backend-go/README.md docker-compose.yml scripts/docker/start-webui.sh</automated>
  </verify>
  <done>The published production path no longer defaults to Python backend ownership.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Audit and dispose of residual Python backend bridges</name>
  <files>backend-go/internal/registration/python_runner.go, backend-go/internal/registration/python_runner_script.go, src/web/routes/payment.py, .planning/phases/05-cutover-and-decommission/05-VERIFICATION.md</files>
  <behavior>
    - Each residual Python backend helper has a disposition: removed, isolated, or accepted as non-critical.
    - No production-critical flow remains implicitly dependent on the Python registration bridge or Python payment backend route logic.
    - Any retained legacy code has explicit guardrails and documentation.
  </behavior>
  <action>Perform a final production-path audit of the remaining Python registration/payment helpers and record their disposition. Where safe, remove or disable them from the production path; otherwise, isolate them behind explicit compatibility boundaries that cannot silently become the backend truth source again.</action>
  <verify>
    <automated>rg -n "python_runner|bridge|abcard_bridge|fallback|legacy" backend-go/internal/registration/python_runner.go backend-go/internal/registration/python_runner_script.go src/web/routes/payment.py .planning/phases/05-cutover-and-decommission/05-VERIFICATION.md</automated>
  </verify>
  <done>Residual Python backend bridges no longer sit ambiguously on the production path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock final sign-off semantics for any retained Python shell</name>
  <files>webui.py, src/web/app.py, .planning/phases/05-cutover-and-decommission/05-VERIFICATION.md</files>
  <behavior>
    - If Python UI/process remains, the verification report explains exactly why it is not backend-critical.
    - Final sign-off cannot confuse "Python still exists" with "Python still owns backend runtime truth".
    - Regression checks fail if production ownership drifts back to Python without documentation.
  </behavior>
  <action>Add the minimum documentation and verification hooks needed to prove any retained Python shell is outside the production backend critical path. Keep this compatibility-safe; do not accidentally turn the phase into a frontend rewrite.</action>
  <verify>
    <automated>rg -n "critical path|presentation shell|legacy reference|non-critical|rollback" webui.py src/web/app.py .planning/phases/05-cutover-and-decommission/05-VERIFICATION.md</automated>
  </verify>
  <done>The milestone can state the final Python role precisely and honestly.</done>
</task>

</tasks>

<verification>
- `rg -n "webui.py|run-api|run-worker|docker-compose|production|rollback" README.md backend-go/README.md docker-compose.yml scripts/docker/start-webui.sh`
- `rg -n "python_runner|bridge|abcard_bridge|fallback|legacy" backend-go/internal/registration/python_runner.go backend-go/internal/registration/python_runner_script.go src/web/routes/payment.py .planning/phases/05-cutover-and-decommission/05-VERIFICATION.md`
- `bash scripts/verify_phase5_cutover.sh`
</verification>

<success_criteria>
- Production no longer defaults to Python-backed backend ownership.
- Residual Python backend helpers are removed or explicitly isolated.
- Final verification can explain exactly why any remaining Python code is not backend-critical.
</success_criteria>

<output>
After completion, finalize `.planning/phases/05-cutover-and-decommission/05-VERIFICATION.md`
</output>
