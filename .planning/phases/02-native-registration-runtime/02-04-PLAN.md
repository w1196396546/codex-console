---
phase: "02-native-registration-runtime"
plan: "04"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - backend-go/internal/registration/ws/task_socket.go
  - backend-go/internal/registration/ws/task_socket_test.go
  - backend-go/internal/registration/ws/batch_socket.go
  - backend-go/internal/registration/ws/batch_socket_test.go
  - backend-go/tests/e2e/jobs_flow_test.go
autonomous: true
requirements:
  - RUN-02
  - COMP-03
user_setup: []
must_haves:
  truths:
    - "Task websocket frames preserve current snapshot, log, control, and status semantics closely enough for existing clients to reconnect and continue without contract drift."
    - "Batch and Outlook batch websocket frames preserve `cancelling`, compatible status payload shape, heartbeat behavior, and incremental log semantics."
    - "The current frontend can reuse `/api/ws/task/{task_uuid}` and `/api/ws/batch/{batch_id}` without a websocket contract rewrite."
  artifacts:
    - path: "backend-go/internal/registration/ws/task_socket.go"
      provides: "Task websocket snapshot/control stream matching current frontend expectations."
    - path: "backend-go/internal/registration/ws/batch_socket.go"
      provides: "Batch websocket snapshot/control stream reused by standard and Outlook batch flows."
    - path: "backend-go/tests/e2e/jobs_flow_test.go"
      provides: "End-to-end websocket compatibility assertions for task and batch channels."
  key_links:
    - from: "backend-go/internal/registration/ws/task_socket.go"
      to: "backend-go/tests/e2e/jobs_flow_test.go"
      via: "task websocket frame shape and control semantics"
      pattern: "task_uuid|status|log_offset|log_next_offset|cancelling"
    - from: "backend-go/internal/registration/ws/batch_socket.go"
      to: "backend-go/tests/e2e/jobs_flow_test.go"
      via: "batch websocket frame shape and control semantics"
      pattern: "batch_id|status|current_index|skipped|log_next_offset|cancelling"
---

<objective>
Complete the Go websocket runtime semantics that current task and batch consumers already depend on.

Purpose: Per D-03/D-05, isolate websocket parity from the larger HTTP/polling compatibility work so the frame shape, control semantics, and reconnect behavior can be implemented and verified without overloading a single execution unit.
Output: Python-compatible task and batch websocket semantics for registration, batch, and Outlook batch flows.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-native-registration-runtime/02-CONTEXT.md
@.planning/phases/02-native-registration-runtime/02-RESEARCH.md
@.planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md
@src/web/routes/websocket.py
@src/web/task_manager.py
@static/js/app.js
@backend-go/internal/registration/ws/task_socket.go
@backend-go/internal/registration/ws/task_socket_test.go
@backend-go/internal/registration/ws/batch_socket.go
@backend-go/internal/registration/ws/batch_socket_test.go
@backend-go/tests/e2e/jobs_flow_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 对齐 task websocket 帧形状与控制语义</name>
  <files>backend-go/internal/registration/ws/task_socket.go, backend-go/internal/registration/ws/task_socket_test.go, backend-go/tests/e2e/jobs_flow_test.go</files>
  <read_first>.planning/phases/02-native-registration-runtime/02-RESEARCH.md, src/web/routes/websocket.py, src/web/task_manager.py, static/js/app.js, backend-go/internal/registration/ws/task_socket.go, backend-go/internal/registration/ws/task_socket_test.go, backend-go/tests/e2e/jobs_flow_test.go</read_first>
  <behavior>
    - Test 1: task websocket sends the same initial status/log hydration contract current clients expect, then advances logs monotonically without replay drift.
    - Test 2: task websocket control replies preserve `cancelling` visibility, compatible message shape, and heartbeat/ping behavior.
    - Test 3: task websocket frames include the fields current clients use for reconnect and fallback behavior.
  </behavior>
  <action>按 D-03 只在现有 `ws/task_socket.go` 基础上补齐帧形状和控制语义，不新建 websocket 路由族。保持 current frontend 已依赖的 snapshot-first、log streaming、ping/pong、pause/resume/cancel 协议，并对齐 research 已确认的差异：`timestamp`、`cancelling` 的 websocket 可见性、任务控制回包 `message`、以及增量日志语义。优先通过 `task_socket_test.go` 与 `jobs_flow_test.go` 的失败用例编码行为，再最小修正实现；不要把 HTTP/polling 或管理/payment/team websocket 迁移带入本 task。</action>
  <verify>cd backend-go && go test ./internal/registration/ws ./tests/e2e -run 'TestTaskSocketSendsCurrentStatusAndLogs|TestTaskSocketCompletedStatusIncludesEmailFromJobResult|TestRegistrationWebSocketCompatibility' -v</verify>
  <acceptance_criteria>
    - task websocket maintains monotonic log streaming and compatible status frames
    - task websocket exposes `cancelling` visibility and compatible control replies
    - targeted ws/e2e tests prove `/api/ws/task/{task_uuid}` remains frontend-compatible
  </acceptance_criteria>
  <done>Go task websocket semantics remain compatible with current client expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 对齐 batch 与 Outlook batch websocket 语义</name>
  <files>backend-go/internal/registration/ws/batch_socket.go, backend-go/internal/registration/ws/batch_socket_test.go, backend-go/tests/e2e/jobs_flow_test.go</files>
  <read_first>.planning/phases/02-native-registration-runtime/02-RESEARCH.md, src/web/routes/websocket.py, src/web/task_manager.py, static/js/app.js, backend-go/internal/registration/ws/batch_socket.go, backend-go/internal/registration/ws/batch_socket_test.go, backend-go/tests/e2e/jobs_flow_test.go</read_first>
  <behavior>
    - Test 1: batch websocket sends compatible snapshot/control/log frames for standard batch flows.
    - Test 2: batch websocket preserves `cancelling`, heartbeat behavior, and incremental log semantics instead of collapsing directly to terminal-only states.
    - Test 3: Outlook batch continues to reuse `/api/ws/batch/{batch_id}` with compatible `skipped/current_index`-aware semantics.
  </behavior>
  <action>按 D-03 只在现有 `ws/batch_socket.go` 基础上补齐批次 websocket 语义，保持标准批次与 Outlook 批次继续复用 `/api/ws/batch/{batch_id}`。对齐当前 research 已确认的差异：batch status payload shape、`timestamp`、`cancelling` 中间态、`skipped/current_index` 的 websocket 可见性、以及 pause/resume/cancel 的即时回包与后续轮询一致性。优先通过 `batch_socket_test.go` 与 `jobs_flow_test.go` 的失败用例编码行为，再最小修正实现；不要把 HTTP/polling 语义回卷到本 task。</action>
  <verify>cd backend-go && go test ./internal/registration/ws ./tests/e2e -run 'TestBatchSocketSendsCurrentStatusAndLogs|TestRegistrationBatchWebSocketCompatibility|TestRegistrationOutlookBatchCompatibility' -v</verify>
  <acceptance_criteria>
    - batch websocket exposes `cancelling` and compatible control replies instead of collapsing directly to a terminal-only view
    - Outlook batch websocket consumption remains on `/api/ws/batch/{batch_id}` with matching semantics
    - targeted ws/e2e tests prove current frontend can reuse batch websocket flows unchanged
  </acceptance_criteria>
  <done>Go batch websocket semantics remain compatible for both standard and Outlook batch consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client -> registration websocket handlers | Client control messages and reconnects can corrupt status/log sequencing if the websocket contract drifts. |
| Batch service snapshot -> websocket frame projection | Missing or reordered fields here break live task monitoring without any route changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | T | task websocket frame shape | mitigate | Lock task frame fields and control replies in ws + e2e tests before changing implementation. |
| T-02-04-02 | D | batch websocket sequencing | mitigate | Keep snapshot-first + monotonic incremental log behavior under unit/e2e coverage. |
| T-02-04-03 | R | Outlook batch websocket reuse | mitigate | Encode the reuse of `/api/ws/batch/{batch_id}` for Outlook batch in e2e tests so later changes cannot silently fork the channel semantics. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd backend-go && go test ./internal/registration/ws ./tests/e2e -run 'TestTaskSocketSendsCurrentStatusAndLogs|TestTaskSocketCompletedStatusIncludesEmailFromJobResult|TestRegistrationWebSocketCompatibility' -v`
- [ ] `cd backend-go && go test ./internal/registration/ws ./tests/e2e -run 'TestBatchSocketSendsCurrentStatusAndLogs|TestRegistrationBatchWebSocketCompatibility|TestRegistrationOutlookBatchCompatibility' -v`
- [ ] Current websocket handlers expose no new Phase 3 or Phase 4 API surface
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Task and batch websocket consumers observe compatible progress/log windows without frontend rewrites
- Outlook batch continues to reuse the existing batch websocket channel with compatible semantics
- No management/payment/team migration work was pulled into the plan
</success_criteria>

<output>
After completion, create `.planning/phases/02-native-registration-runtime/02-04-SUMMARY.md`
</output>
