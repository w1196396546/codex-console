---
phase: 04-payment-and-team-domains
plan: "09"
type: execute
wave: 4
depends_on: ["04-07"]
files_modified:
  - backend-go/internal/payment/transition_adapters.go
  - backend-go/internal/payment/transition_adapters_test.go
  - backend-go/cmd/api/payment_runtime_test.go
autonomous: true
gap_closure: true
requirements: [PAY-01]
must_haves:
  truths:
    - "Transition random billing profiles must stay internally consistent for every country the adapter claims to support."
    - "Synthetic checkout session IDs and checkout URLs must remain URL-safe for arbitrary workspace names."
    - "Regression tests fail if unsupported address/currency combinations or broken checkout links reappear."
  artifacts:
    - "backend-go/internal/payment/transition_adapters.go uses country templates that match every advertised supported country or falls back consistently."
    - "backend-go/internal/payment/transition_adapters.go normalizes workspace-derived checkout session IDs into URL-safe values."
    - "backend-go/internal/payment/transition_adapters_test.go and backend-go/cmd/api/payment_runtime_test.go cover non-ASCII and slash-containing workspace names plus non-US/GB/CA billing countries."
  key_links:
    - "The adapter must not emit `country=AU` with a US/CA postal/state combination."
    - "The adapter must not emit checkout links whose path semantics break when `workspace_name` contains `/ ? # %` or non-ASCII text."
---

<objective>
Close the remaining payment compatibility gaps in random billing profile generation and checkout link normalization.

Purpose: Code review found that the current payment transition adapter still emits invalid address/currency combinations for several advertised countries and builds synthetic checkout links from raw workspace names without URL-safe normalization.
Output: corrected payment transition normalization plus regression tests that lock the supported-country and workspace-name semantics.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-payment-and-team-domains/04-07-SUMMARY.md
@backend-go/internal/payment/transition_adapters.go
@backend-go/internal/payment/transition_adapters_test.go
@backend-go/cmd/api/payment_runtime_test.go
@src/core/openai/random_billing.py
@src/core/openai/payment.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align supported-country billing templates with the compatibility oracle</name>
  <files>backend-go/internal/payment/transition_adapters.go, backend-go/internal/payment/transition_adapters_test.go, backend-go/cmd/api/payment_runtime_test.go</files>
  <behavior>
    - Test 1: every country the adapter advertises as supported returns a self-consistent local billing profile for that same country.
    - Test 2: unsupported countries still fall back deterministically to the documented default.
  </behavior>
  <action>Reuse the Python compatibility oracle in `src/core/openai/random_billing.py` to either add missing local templates for the countries already listed in the Go adapter or narrow the advertised support to the countries with valid templates. Keep the returned `country/currency/state/postal_code` fields internally consistent.</action>
  <verify>
    <automated>cd backend-go && go test ./internal/payment -run 'TestPaymentTransitionAdapter.*' -v && go test ./cmd/api -run 'TestAPIPaymentRuntime.*' -v</automated>
  </verify>
  <done>Transition random billing generation no longer emits country/template mismatches for supported countries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize workspace-derived checkout session IDs into URL-safe values</name>
  <files>backend-go/internal/payment/transition_adapters.go, backend-go/internal/payment/transition_adapters_test.go, backend-go/cmd/api/payment_runtime_test.go</files>
  <behavior>
    - Test 1: slash, percent, question-mark, hash, and non-ASCII workspace names still produce a stable URL-safe checkout link/session id.
    - Test 2: simple ASCII workspace names keep their current compatibility shape.
  </behavior>
  <action>Normalize the workspace component used in synthetic checkout session IDs so generated links remain URL-safe and deterministic for arbitrary workspace names. Keep the session id human-inspectable enough for debugging, but do not allow it to break path parsing.</action>
  <verify>
    <automated>cd backend-go && go test ./internal/payment -run 'TestPaymentTransitionAdapter.*' -v && go test ./cmd/api -run 'TestAPIPaymentRuntime.*' -v</automated>
  </verify>
  <done>Transition checkout links stay valid and deterministic for arbitrary workspace names.</done>
</task>

</tasks>

<verification>
- `cd backend-go && go test ./internal/payment -run 'TestPaymentTransitionAdapter.*' -v`
- `cd backend-go && go test ./cmd/api -run 'TestAPIPaymentRuntime.*' -v`
</verification>

<success_criteria>
- Supported-country billing profiles are internally consistent.
- Synthetic checkout links remain URL-safe for arbitrary workspace names.
</success_criteria>

<output>
After completion, create `.planning/phases/04-payment-and-team-domains/04-09-SUMMARY.md`
</output>
