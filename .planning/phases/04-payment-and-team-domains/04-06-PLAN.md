---
phase: 04-payment-and-team-domains
plan: "06"
type: execute
wave: 2
depends_on: ["04-03", "04-04", "04-05"]
files_modified:
  - backend-go/cmd/api/main.go
  - backend-go/internal/http/router_test.go
  - backend-go/tests/e2e/payment_team_flow_test.go
autonomous: true
gap_closure: true
requirements: [PAY-01, TEAM-01]
must_haves:
  truths:
    - "The live API bootstrap wires the new payment and team runtime helpers, so mounted Phase 4 routes no longer rely on hollow constructor defaults."
    - "Router and e2e coverage prove the current bootstrap path instead of passing only because tests injected fake payment/team seams by hand."
    - "Phase 4 closes the concrete runtime blockers without pulling any Phase 5 cutover, rollout, or operator-runbook work forward."
  artifacts:
    - "backend-go/cmd/api/main.go consumes the helper-built payment and team runtimes from 04-04 and 04-05."
    - "backend-go/internal/http/router_test.go asserts the mounted routes stay owned after the runtime helpers are wired in."
    - "backend-go/tests/e2e/payment_team_flow_test.go uses the helper-built runtime path and proves payment smoke flows plus Team accepted-task live execution under Phase 4 bootstrap semantics."
  key_links:
    - "`backend-go/cmd/api/main.go` replaces direct hollow constructor calls with the helper builders introduced by 04-04 and 04-05."
    - "Phase 4 regression coverage no longer requires direct fake seam injection or manual out-of-band Team execution to prove mounted behavior."
---

<objective>
Wire the new payment and team runtime helpers into the live API bootstrap, then convert the Phase 4 regression coverage so it proves the real constructor path is closed.

Purpose: 04-04 and 04-05 close the domain-level runtime seams, but the shared `cmd/api/main.go` entrypoint is still the final point of failure. This plan is the minimal integration step needed to close both blockers.
Output: updated API bootstrap wiring plus router/e2e tests that exercise the live Phase 4 constructor path.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-payment-and-team-domains/04-VERIFICATION.md
@.planning/phases/04-payment-and-team-domains/04-03-SUMMARY.md
@.planning/phases/04-payment-and-team-domains/04-04-PLAN.md
@.planning/phases/04-payment-and-team-domains/04-05-PLAN.md
@backend-go/cmd/api/main.go
@backend-go/internal/http/router.go
@backend-go/internal/http/router_test.go
@backend-go/tests/e2e/payment_team_flow_test.go
@static/js/payment.js
@static/js/accounts.js
@static/js/auto_team.js

<interfaces>
Current hollow call sites in `main.go`:
```go
paymentService := payment.NewService(payment.NewPostgresRepository(deps.Postgres), accountsRepository)
teamService := team.NewService(teamRepository, nil)
teamTaskService := team.NewTaskService(teamRepository, teamService, jobService, nil)
```

Existing mounted surfaces that must stay stable:
```go
r.Get("/api/ws/task/{task_uuid}", taskSocketHandler.HandleTaskSocket)
// `/api/payment*` and `/api/team*` stay on the same `/api/*` contract
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace hollow payment/team constructor calls in `cmd/api/main.go` with runtime helpers</name>
  <files>backend-go/cmd/api/main.go</files>
  <behavior>
    - Test 1: `main.go` consumes the helper-built payment runtime from 04-04 and the helper-built team runtime from 04-05, so no nil payment/team live seam remains in the mounted API process.
    - Test 2: existing `/api/*` route ownership and shared `/api/ws/task/{task_uuid}` semantics remain unchanged.
  </behavior>
  <action>Per D-04, D-05, D-06, and D-07, update `backend-go/cmd/api/main.go` to replace the direct hollow constructor calls with the helper builders introduced in 04-04 and 04-05. Keep the current route ownership additive on `/api/*`, keep the shared task websocket mount untouched, and do not add cutover flags, rollout branches, or any Phase 5 behavior. This task should only integrate the already-prepared runtime helpers into the live bootstrap.</action>
  <verify>
    <automated>cd backend-go && go test ./cmd/api -run 'TestAPI(Payment|Team)Runtime.*' -v</automated>
  </verify>
  <done>`cmd/api/main.go` no longer mounts payment/team with hollow constructor defaults, and the integration point remains Phase 4-scoped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh router and e2e coverage to prove the real Phase 4 bootstrap path</name>
  <files>backend-go/internal/http/router_test.go, backend-go/tests/e2e/payment_team_flow_test.go</files>
  <behavior>
    - Test 1: router/e2e coverage fails if payment routes regress to `not configured` runtime errors or Team accepted work stays at `pending` under the helper-built constructor path.
    - Test 2: the revised Phase 4 e2e setup no longer depends on direct fake seam injection or manual out-of-band `taskService.ExecuteTask(...)` calls to prove live behavior.
    - Test 3: payment smoke routes and Team accepted-task live flow still preserve the current `static/js` contract and `/api/ws/task/{task_uuid}` projection semantics.
  </behavior>
  <action>Rewrite the Phase 4 router/e2e tests so they build services through the helper runtime path instead of hand-injecting fake seams straight into `payment.NewService(...)` or manually driving Team execution from the test body. Keep the assertions anchored to the current consumer contract from `static/js/payment.js`, `static/js/accounts.js`, and `static/js/auto_team.js`: payment must exercise generate-link/random-billing/session-bootstrap/subscription-sync style smoke paths, and Team must show accepted-task execution advancing beyond pending on the shared websocket path. Do not expand this into human staging validation or Phase 5 rollout work.</action>
  <verify>
    <automated>cd backend-go && go test ./internal/http -run 'Test(Router|PaymentTeam|PhaseFour).*' -v && go test ./tests/e2e -run 'Test(Payment|Team|PhaseFour).*' -v</automated>
  </verify>
  <done>Automated Phase 4 regression coverage now proves the actual bootstrap wiring is closed for both payment and team domains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API bootstrap→payment/team runtime helpers | `cmd/api/main.go` is the final shared integration point for the new runtime helpers. |
| router/e2e coverage→mounted Phase 4 routes | Regression tests must exercise the real constructor path rather than isolated fake seams. |
| Team accepted-task flow→shared websocket runtime | Team execution remains observable through the shared `/api/ws/task/{task_uuid}` channel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | T | `cmd/api/main.go` wiring | mitigate | Replace the hollow constructor calls with explicit helper builders and guard them with `cmd/api` runtime tests. |
| T-04-06-02 | S | route-ownership regression tests | mitigate | Update router/e2e coverage so mounted behavior is proven through the actual bootstrap path, preventing false confidence from fake direct injection. |
| T-04-06-03 | D | Team live task progression | mitigate | Require e2e to observe pending→running/completed movement on the shared websocket path without manual external execution calls. |
</threat_model>

<verification>
- `cd backend-go && go test ./cmd/api -run 'TestAPI(Payment|Team)Runtime.*' -v`
- `cd backend-go && go test ./internal/http -run 'Test(Router|PaymentTeam|PhaseFour).*' -v`
- `cd backend-go && go test ./tests/e2e -run 'Test(Payment|Team|PhaseFour).*' -v`
</verification>

<success_criteria>
- The mounted API process uses the new payment/team runtime helpers instead of hollow defaults.
- Router and e2e coverage now prove real bootstrap closure for both blockers.
- Phase 5 cutover and rollout remain untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/04-payment-and-team-domains/04-06-SUMMARY.md`
</output>
