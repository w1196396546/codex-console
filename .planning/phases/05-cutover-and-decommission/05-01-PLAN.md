---
phase: 05-cutover-and-decommission
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - backend-go/README.md
  - backend-go/docs/phase1-runbook.md
  - docker-compose.yml
  - scripts/docker/start-webui.sh
  - scripts/verify_phase5_cutover.sh
  - .planning/phases/05-cutover-and-decommission/05-VERIFICATION.md
autonomous: true
requirements: [CUT-02]
must_haves:
  truths:
    - "Operators must have one explicit Go-owned backend cutover topology with required env/services, startup order, and rollback steps."
    - "Phase 2-4 deferred live/operator checks must be pulled into a single Phase 5 compatibility evidence gate before final sign-off."
    - "Phase 5 must never claim cutover readiness from stale docs or local-only unit tests."
  artifacts:
    - "Published docs and runbooks describe Go API + worker as the target backend runtime and explain how rollback works."
    - "An environment-gated verification entrypoint exists for Phase 5 cutover evidence."
    - "A Phase 5 verification report can cite concrete evidence for topology, rollback, and deferred operator workflows."
  key_links:
    - "`README.md`, `backend-go/README.md`, and the runtime scripts must agree on the production topology."
    - "Phase 5 verification must connect prior phase proofs to new live/operator evidence instead of re-testing domains in isolation."
---

<objective>
Define and verify the staged Go cutover path: topology, prerequisites, rollback, and compatibility evidence. This plan closes the operational ambiguity left after the domain migrations by making the Go-owned runtime the documented target backend path and by adding a bounded verification gate for final sign-off.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cutover-and-decommission/05-CONTEXT.md
@.planning/phases/05-cutover-and-decommission/05-RESEARCH.md
@.planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md
@.planning/phases/01-compatibility-baseline/01-ops-compat-runbook.md
@.planning/phases/04-payment-and-team-domains/04-VERIFICATION.md
@README.md
@backend-go/README.md
@backend-go/docs/phase1-runbook.md
@webui.py
@src/web/app.py
@scripts/docker/start-webui.sh
@docker-compose.yml
@backend-go/cmd/api/main.go
@backend-go/cmd/worker/main.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Publish the Go-owned cutover topology and rollback contract</name>
  <files>README.md, backend-go/README.md, backend-go/docs/phase1-runbook.md, docker-compose.yml, scripts/docker/start-webui.sh</files>
  <behavior>
    - Document the target production backend topology, required env/services, startup order, and rollback path.
    - Remove stale statements that still describe payment/team as not migrated or otherwise imply Python still owns the backend path.
    - Make it explicit whether Python remains as a temporary presentation shell, proxy client, or legacy-only tool after cutover.
  </behavior>
  <action>Create or update operator-facing docs and runtime manifests so the production target path is unambiguous: Go API + Go worker with PostgreSQL and Redis. Keep rollback instructions concrete and consistent across all published surfaces.</action>
  <verify>
    <automated>rg -n "run-api|run-worker|rollback|Go-owned|production backend|Python" README.md backend-go/README.md backend-go/docs/phase1-runbook.md docker-compose.yml scripts/docker/start-webui.sh</automated>
  </verify>
  <done>The repo publishes one coherent cutover story instead of a Python-first deployment story.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an environment-gated Phase 5 cutover verification entrypoint</name>
  <files>scripts/verify_phase5_cutover.sh, backend-go/Makefile, .planning/phases/05-cutover-and-decommission/05-VERIFICATION.md</files>
  <behavior>
    - There is one explicit script or command entrypoint for final cutover evidence.
    - Local checks remain useful without a live environment, but live/operator checks are clearly gated and never silently skipped as “pass”.
    - The verification artifact records what was proven locally versus what still requires a live target.
  </behavior>
  <action>Add a Phase 5 verification harness that combines existing local Go tests with environment-gated live checks for the final cutover. The harness must fail loudly on real mismatches and must clearly label gated/skipped live checks instead of overstating readiness.</action>
  <verify>
    <automated>bash scripts/verify_phase5_cutover.sh</automated>
  </verify>
  <done>Phase 5 has an evidence-first validation entrypoint suitable for final migration sign-off.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Consolidate deferred operator validation into a single sign-off checklist</name>
  <files>.planning/phases/05-cutover-and-decommission/05-VERIFICATION.md, backend-go/docs/phase1-runbook.md</files>
  <behavior>
    - Phase 2-4 deferred live checks are pulled into Phase 5 instead of being left as scattered notes.
    - The checklist names exact workflow classes to validate before sign-off.
    - The verification report distinguishes "ready for cutover" from "cutover completed".
  </behavior>
  <action>Translate the earlier deferred validations into one bounded Phase 5 checklist and reporting format so final sign-off is auditable. The output should cite the earlier verification artifacts but also require fresh Phase 5 evidence where live/operator workflows matter.</action>
  <verify>
    <automated>rg -n "Phase 2|Phase 3|Phase 4|operator|live|rollback|sign-off" .planning/phases/05-cutover-and-decommission/05-VERIFICATION.md backend-go/docs/phase1-runbook.md</automated>
  </verify>
  <done>The milestone has a single final cutover checklist instead of scattered deferred-validation notes.</done>
</task>

</tasks>

<verification>
- `rg -n "run-api|run-worker|rollback|Go-owned|production backend|Python" README.md backend-go/README.md backend-go/docs/phase1-runbook.md docker-compose.yml scripts/docker/start-webui.sh`
- `bash scripts/verify_phase5_cutover.sh`
- `cd backend-go && go test ./internal/http ./tests/e2e -run 'Test(HealthzEndpoint|Payment|Team|Management|Registration).*' -v`
</verification>

<success_criteria>
- Operators have a documented Go-owned cutover topology with rollback.
- Phase 5 has one explicit, evidence-first cutover verification gate.
- Deferred live/operator validations from earlier phases are consolidated into final sign-off evidence.
</success_criteria>

<output>
After completion, update `.planning/phases/05-cutover-and-decommission/05-VERIFICATION.md`
</output>
