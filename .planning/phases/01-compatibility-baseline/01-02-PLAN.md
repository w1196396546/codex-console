---
phase: "01-compatibility-baseline"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-compatibility-baseline/01-shared-schema-contract.md
  - .planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md
autonomous: true
requirements:
  - DATA-01
  - COMP-02
user_setup: []
must_haves:
  truths:
    - "Phase 1 has an explicit table-and-column contract for shared, Python-only, and transitional persistence surfaces."
    - "The migration baseline records the current auth boundary, runtime ownership split, and task/websocket semantics before later phases change implementation."
    - "Later plans can tell which data/runtime behaviors must be preserved and which infrastructure prerequisites apply to Go cutover."
  artifacts:
    - path: ".planning/phases/01-compatibility-baseline/01-shared-schema-contract.md"
      provides: "Table-by-table schema contract for Python ORM models versus Go migrations, including compatibility gaps and storage policy."
    - path: ".planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md"
      provides: "Auth boundary, runtime ownership baseline, task/batch semantics, and Go cutover prerequisite contract."
  key_links:
    - from: ".planning/phases/01-compatibility-baseline/01-shared-schema-contract.md"
      to: ".planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md"
      via: "shared references to Python-only tables, Go prerequisites, and preserved status/log semantics"
      pattern: "registration_tasks|bind_card_tasks|teams|DATABASE_URL|REDIS_ADDR|log_next_offset"
---

<objective>
Freeze the shared data and runtime compatibility contract that later migration phases must obey.

Purpose: Capture the storage, auth, and runtime rules around the remaining Python-owned domains per D-01/D-02/D-06, while preserving the already-migrated Go baseline without re-planning it per D-03/D-04.
Output: A schema contract for persisted records and a runtime/auth boundary contract for later cutover planning.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compatibility-baseline/01-CONTEXT.md
@.planning/phases/01-compatibility-baseline/01-RESEARCH.md
@src/database/models.py
@src/database/team_models.py
@src/database/session.py
@backend-go/db/migrations/0001_init_jobs.sql
@backend-go/db/migrations/0002_init_accounts_registration.sql
@backend-go/db/migrations/0003_extend_registration_service_configs.sql
@backend-go/internal/config/config.go
@src/web/app.py
@src/web/task_manager.py
@src/web/routes/websocket.py
@backend-go/internal/http/router.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固化共享 schema contract 与存储策略</name>
  <files>.planning/phases/01-compatibility-baseline/01-shared-schema-contract.md</files>
  <read_first>src/database/models.py, src/database/team_models.py, src/database/session.py, backend-go/db/migrations/0001_init_jobs.sql, backend-go/db/migrations/0002_init_accounts_registration.sql, backend-go/db/migrations/0003_extend_registration_service_configs.sql, backend-go/internal/config/config.go</read_first>
  <action>创建 `.planning/phases/01-compatibility-baseline/01-shared-schema-contract.md`，以 Python ORM 为兼容参考（D-01），逐表对照 Go 迁移，明确 Go 不得静默改变的记录形状（D-02/D-06）。文档必须包含一个总览表，列名固定为 `Table`, `Current owner`, `Go coverage`, `Storage policy`, `Compatibility gaps`, `Phase handoff`，并逐节覆盖 `accounts`, `email_services`, `settings`, `cpa_services`, `sub2api_services`, `tm_services`, `registration_tasks`, `bind_card_tasks`, `app_logs`, `proxies`, `teams`, `team_memberships`, `team_tasks`, `team_task_items`。在 `Compatibility gaps` 中显式写出研究已确认的差异：`settings.description/category/updated_at` 缺口、`email_services.last_used` 缺口，以及所有 `team_*`、`bind_card_tasks`、`app_logs`、`proxies` 仍为 Python-only。`Storage policy` 小节必须原样写出这句英文：`SQLite remains a Python reference input, not the Go production path`。同时写明 Go 运行前提是 `DATABASE_URL` + `REDIS_ADDR`，后续阶段必须提供明确迁移/接管步骤。不要修改现有迁移 SQL。</action>
  <verify>rg -n "^## accounts$|^## bind_card_tasks$|settings.description/category/updated_at|email_services.last_used|SQLite.*reference|DATABASE_URL|REDIS_ADDR" .planning/phases/01-compatibility-baseline/01-shared-schema-contract.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-compatibility-baseline/01-shared-schema-contract.md` contains `settings.description/category/updated_at`
    - `.planning/phases/01-compatibility-baseline/01-shared-schema-contract.md` contains `email_services.last_used`
    - `.planning/phases/01-compatibility-baseline/01-shared-schema-contract.md` contains `## bind_card_tasks`
    - `.planning/phases/01-compatibility-baseline/01-shared-schema-contract.md` contains `SQLite remains a Python reference input, not the Go production path`
  </acceptance_criteria>
  <done>The schema contract names every shared or Python-only table, records concrete field-level gaps, and states the Go storage prerequisites unambiguously.</done>
</task>

<task type="auto">
  <name>Task 2: 固化 auth 边界、运行时归属与任务语义 contract</name>
  <files>.planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md</files>
  <read_first>src/web/app.py, src/web/task_manager.py, src/web/routes/websocket.py, src/web/routes/registration.py, backend-go/internal/http/router.go, backend-go/internal/jobs/service.go, backend-go/internal/registration/batch_service.go, backend-go/README.md</read_first>
  <action>创建 `.planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md`，把当前运行时和访问边界冻结为后续阶段必须参考的合同（D-02/D-06）。文档必须包含以下固定小节：`## Auth Boundary`, `## Runtime Ownership Baseline`, `## Task and Batch Semantics`, `## Go Cutover Preconditions`, `## Decision Traceability`。在 `Auth Boundary` 中写明当前页面级鉴权规则：除 `/payment` 外的大多数 HTML 页面依赖 `webui_auth` cookie，`/api` 与 `/api/ws` 当前没有统一鉴权中间件；把这一点标为 “current compatibility reference, not a hardening endorsement”。在 `Runtime Ownership Baseline` 中写明 D-03 基线：现有 Go jobs、registration/task APIs、websocket streams、account-listing baseline、uploader persistence、native runner foundations 属于既有基线，不在 Phase 1 重做。`Task and Batch Semantics` 必须列出至少这些兼容字段或通道：`task_uuid`, `status`, `logs`, `log_offset`, `log_next_offset`, `/api/ws/task/{task_uuid}`, `/api/ws/batch/{batch_id}`。`Go Cutover Preconditions` 必须写明 Go 接管需要 PostgreSQL 与 Redis，并把当前 SQLite 路径视为 legacy reference only。不要改现有 Python/Go 实现，只输出合同文本。</action>
  <verify>rg -n "^## Auth Boundary$|^## Runtime Ownership Baseline$|^## Task and Batch Semantics$|^## Go Cutover Preconditions$|webui_auth|/api/ws/task/\\{task_uuid\\}|log_next_offset|legacy reference only" .planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md` contains `webui_auth`
    - `.planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md` contains `/api/ws/batch/{batch_id}`
    - `.planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md` contains `current compatibility reference, not a hardening endorsement`
    - `.planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md` contains `legacy reference only`
  </acceptance_criteria>
  <done>The runtime contract explicitly freezes current auth exposure, task semantics, and Go cutover prerequisites so later phases cannot make silent assumptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python ORM and runtime behavior -> migration contract | Field or semantic omissions here would cause irreversible data or behavior drift later. |
| Auth/runtime observations -> later cutover plans | Misstating current exposure could produce unsafe or breaking changes in later phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | Shared schema contract | mitigate | Require explicit coverage of every relevant table and the known gap strings (`settings.description/category/updated_at`, `email_services.last_used`, `team_*`, `bind_card_tasks`, `app_logs`, `proxies`) with grep-verifiable acceptance criteria. |
| T-02-02 | E | Auth boundary contract | mitigate | Document the exact current page/API exposure from `src/web/app.py` and label it as compatibility reference only, preventing silent privilege changes from being treated as harmless cleanup. |
| T-02-03 | D | Go cutover prerequisites | mitigate | State `DATABASE_URL` and `REDIS_ADDR` as mandatory Go runtime preconditions and mark SQLite as legacy reference only, so future execution cannot assume a nonexistent single-file fallback. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `rg -n "^## accounts$|^## teams$|SQLite remains a Python reference input, not the Go production path" .planning/phases/01-compatibility-baseline/01-shared-schema-contract.md`
- [ ] `rg -n "settings.description/category/updated_at|email_services.last_used|bind_card_tasks|team_task_items" .planning/phases/01-compatibility-baseline/01-shared-schema-contract.md`
- [ ] `rg -n "^## Auth Boundary$|webui_auth|/api/ws/task/\\{task_uuid\\}|log_offset|log_next_offset|DATABASE_URL|REDIS_ADDR" .planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The shared schema contract explicitly covers Python-only and shared persistence surfaces without assuming Go already owns them
- The runtime boundary contract freezes current auth/task semantics and Go prerequisites for later migration phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-compatibility-baseline/01-02-SUMMARY.md`
</output>
