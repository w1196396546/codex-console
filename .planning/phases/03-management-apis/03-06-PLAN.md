---
phase: 03-management-apis
plan: "06"
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05"]
files_modified:
  - backend-go/cmd/api/main.go
  - backend-go/internal/http/router.go
  - backend-go/internal/http/router_test.go
  - backend-go/tests/e2e/accounts_flow_test.go
  - backend-go/tests/e2e/management_flow_test.go
autonomous: true
requirements: [CUT-01, MGMT-01, MGMT-02]
must_haves:
  truths:
    - "现有 templates/static JS 可以在不改前端结构的情况下直接打到 Go-backed management APIs。"
    - "accounts/settings/email-services/upload-configs/logs 路由全部由 Go 接管，而 payment/team 路由继续保持 Phase 4 owner。"
  artifacts:
    - "backend-go/cmd/api/main.go 构建并注入 Phase 3 新增的 management services。"
    - "backend-go/internal/http/router.go 在现有 `/api/*` 路径上挂载所有 Phase 3 管理域 handlers。"
    - "backend-go/tests/e2e/management_flow_test.go` 锁定当前页面 consumer 的关键路由族和 mixed envelope。"
  key_links:
    - "Go router 只接管 Phase 3 范围内的 management APIs，不改 `/api/payment/*` 与 `/api/team/*` 路由 owner。"
    - "e2e 覆盖直接基于 parity matrix 与当前页面 consumer，而不是引入前端重写。"
---

<objective>
把 Phase 3 新增的各个 management slice 接入现有 Go API 进程，并用契约测试证明当前前端页面可以直接切换到 Go backend。

Purpose: 收口 `CUT-01`，同时把 `MGMT-01` / `MGMT-02` 从“有实现”推进到“现有 UI 可直接消费”。
Output: API bootstrap/router wiring、management e2e compatibility tests、Phase 3 全域路由接管清单。
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-management-apis/03-CONTEXT.md
@.planning/phases/03-management-apis/03-RESEARCH.md
@.planning/phases/03-management-apis/03-UI-SPEC.md
@.planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md
@.planning/phases/02-native-registration-runtime/02-02-SUMMARY.md
@.planning/phases/02-native-registration-runtime/02-04-SUMMARY.md
@backend-go/cmd/api/main.go
@backend-go/internal/http/router.go
@static/js/accounts.js
@static/js/accounts_overview.js
@static/js/settings.js
@static/js/email_services.js
@static/js/logs.js

<interfaces>
Current Go bootstrap seam from `backend-go/cmd/api/main.go`:
```go
jobService := jobs.NewService(...)
registrationService := registration.NewService(jobService)
accountsService := accounts.NewService(accounts.NewPostgresRepository(deps.Postgres))
internalhttp.NewRouter(jobService, registrationService, batchService, availableServices, statsService, outlookService, accountsService, taskSocketHandler, batchSocketHandler)
```

Current router mount style from `backend-go/internal/http/router.go`:
```go
if accountsService != nil {
	accountshttp.NewHandler(accountsService).RegisterRoutes(r)
}
if registrationService != nil && jobService != nil {
	registrationhttp.NewHandler(...).RegisterRoutes(r)
}
```

Management consumer constraint:
```text
保留当前 templates/static-js contract；不新增 SPA/router，不迁移 `/api/payment/*`、`/api/team/*`。
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 API bootstrap/router 中挂载 Phase 3 管理域 handlers</name>
  <files>backend-go/cmd/api/main.go, backend-go/internal/http/router.go, backend-go/internal/http/router_test.go</files>
  <behavior>
    - Test 1: API 进程会构建并注入 settings、emailservices、uploader admin、logs 与扩展后的 accounts service，且所有路由仍保持当前 `/api/*` 路径。
    - Test 2: router 只接管 Phase 3 范围内的 management APIs；`/api/payment/*` 和 `/api/team/*` 不在本计划中新增 Go mounts。
  </behavior>
  <action>按 D-02/D-03/D-04/D-05 把 03-01 到 03-05 产出的 services/handlers 接到 `cmd/api/main.go` 和 `internal/http/router.go`。必须沿用当前 router mount 风格和路径，不新增管理前缀，也不碰 payment/team owner。router tests 需要显式锁定 Phase 3 路由存在、Phase 4 路由未被提前接管。</action>
  <verify>
    <automated>cd backend-go && go test ./internal/http -run 'Test(Router|Healthz|Management).*' -v</automated>
  </verify>
  <done>Go API 进程已经注册所有 Phase 3 管理域路由，且 scope 边界对 payment/team 仍然清晰。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 增加 management e2e compatibility tests，锁定当前 UI consumer 契约</name>
  <files>backend-go/tests/e2e/accounts_flow_test.go, backend-go/tests/e2e/management_flow_test.go</files>
  <behavior>
    - Test 1: accounts/settings/email-services/upload-configs/logs 的关键页面 consumer 通过当前 `/api/*` 路径和 mixed envelope 读取 Go 响应而不需要改模板/静态 JS。
    - Test 2: payment/team 相关路径仍不在 Go management e2e 覆盖中，确保 Phase 3 不偷偷越界。
  </behavior>
  <action>基于 Phase 1 parity matrix 和当前 `static/js/*.js` consumer，为管理域新增一套 Go e2e compatibility tests。重点锁定 list/detail/stats/full/plain-array/object-envelope 等当前页面混合协议，不写“新的 UI 测试页面”，也不改现有模板/JS。必要时扩展现有 `accounts_flow_test.go`，并新增 `management_flow_test.go` 覆盖 settings/email/upload/logs contract。</action>
  <verify>
    <automated>cd backend-go && go test ./tests/e2e -run 'Test(RecentAccountsCompatibilityEndpoint|Management).*' -v</automated>
  </verify>
  <done>Phase 3 管理域已经有 router + e2e 证据证明现有 UI consumer 可直接切到 Go backend，而无需前端重写。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/static-js→Go management router | 现有页面会直接把既有请求协议发送到 Go 管理路由 |
| api bootstrap→domain services | 错误 wiring 会导致路由漂移或把不该接管的域提前暴露 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | S | router wiring | mitigate | 通过 router tests 明确锁定 Phase 3 管理域已挂载、Phase 4 payment/team 未挂载，避免错误 owner 冒充。 |
| T-03-06-02 | T | current UI contract | mitigate | e2e 直接基于 parity matrix 和当前静态 JS 路径/字段写断言，防止接线后因 envelope/字段漂移造成隐性破坏。 |
| T-03-06-03 | I | mixed management responses | mitigate | 在 e2e 中锁定 plain-array 与 object-envelope 的现有差异，避免 cutover 时把 list/full/stats 返回统一成新的格式。 |
</threat_model>

<verification>
- `cd backend-go && go test ./internal/http -run 'Test(Router|Management).*' -v`
- `cd backend-go && go test ./tests/e2e -run 'Test(RecentAccountsCompatibilityEndpoint|Management).*' -v`
</verification>

<success_criteria>
- Phase 3 管理域由 Go API 进程统一接线并保持当前 `/api/*` 路径。
- accounts/settings/email-services/upload-configs/logs 当前页面 consumer 可直接切到 Go backend，payment/team 仍明确留给 Phase 4。
</success_criteria>

<output>
After completion, create `.planning/phases/03-management-apis/03-management-apis-06-SUMMARY.md`
</output>
