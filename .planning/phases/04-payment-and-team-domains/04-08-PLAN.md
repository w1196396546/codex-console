---
phase: 04-payment-and-team-domains
plan: "08"
type: execute
wave: 3
depends_on: ["04-05", "04-06"]
files_modified:
  - backend-go/internal/team/repository.go
  - backend-go/internal/team/repository_postgres.go
  - backend-go/internal/team/transition_executor.go
  - backend-go/internal/team/transition_executor_test.go
  - backend-go/internal/team/tasks_test.go
  - backend-go/cmd/api/team_runtime.go
  - backend-go/cmd/api/team_runtime_test.go
  - backend-go/tests/e2e/payment_team_flow_test.go
autonomous: true
gap_closure: true
requirements: [TEAM-01]
must_haves:
  truths:
    - "Live Team runtime must perform real transition discovery/sync/invite side effects against the upstream Team API contract instead of completing placeholder local-only work."
    - "Team accepted-task success must remain observable through the shared websocket/task contract while also mutating persisted Team data in a way that matches the current Python oracle."
    - "Regression coverage must fail if discovery/sync/invite silently degrade back to placeholder execution."
  artifacts:
    - "backend-go/internal/team/transition_executor.go uses real transition hooks/clients for discovery, sync, and invite instead of the current local placeholder defaults."
    - "backend-go/cmd/api/team_runtime.go wires those live transition hooks into the current API constructor path."
    - "backend-go/tests/e2e/payment_team_flow_test.go asserts Team side effects, not just completed status/log presence."
    - "backend-go/internal/team/tasks_test.go remains stable under auto-launch and no longer depends on scheduler timing for discovery reuse."
  key_links:
    - "`newAPITeamServices(...)` must no longer mount a placeholder executor that only recounts local state."
    - "Team e2e must prove persisted team/membership/task changes under the helper/runtime path."
---

<objective>
Replace the placeholder Team transition executor with upstream-aware transition behavior and strengthen regression coverage so Phase 4 only closes when real Team side effects occur.

Purpose: 04-05/04-06 closed nil-seam/runtime wiring gaps, but code review found that the default Team executor still synthesizes success without performing true discovery/sync/invite work. This plan converts that seam into a real transition implementation and upgrades tests accordingly.
Output: live Team transition hooks, stronger e2e/task tests, and removal of placeholder-success behavior.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-payment-and-team-domains/04-05-PLAN.md
@.planning/phases/04-payment-and-team-domains/04-05-SUMMARY.md
@.planning/phases/04-payment-and-team-domains/04-06-SUMMARY.md
@backend-go/internal/team/transition_gateway.go
@backend-go/internal/team/transition_executor.go
@backend-go/internal/team/tasks.go
@backend-go/cmd/api/team_runtime.go
@backend-go/tests/e2e/payment_team_flow_test.go
@src/services/team/client.py
@src/services/team/discovery.py
@src/services/team/sync.py
@src/services/team/invite.py
@src/services/team/tasks.py
@src/services/team/runner.py
@static/js/auto_team.js

<review_findings>
- Review finding A: current live Team runtime mounts the default transition executor with no real hooks, so accepted discovery/sync/invite can complete without upstream side effects.
- Review finding B: Team e2e only checks task completion/logs/items and does not verify team/membership persistence changes.
- Review finding C: `TestDiscoveryAcceptedTaskReusesOwnerScopeAndWsChannel` became scheduler-dependent after auto-launch.
</review_findings>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace placeholder Team executor behavior with upstream-aware transition hooks</name>
  <files>backend-go/internal/team/repository.go, backend-go/internal/team/repository_postgres.go, backend-go/internal/team/transition_executor.go, backend-go/internal/team/transition_executor_test.go, backend-go/cmd/api/team_runtime.go, backend-go/cmd/api/team_runtime_test.go</files>
  <behavior>
    - Test 1: discovery uses the upstream Team account check contract and can persist newly discovered Team rows, not only recount preexisting local rows.
    - Test 2: sync uses upstream member/invite payloads and refreshes persisted membership/team aggregates compatibly.
    - Test 3: invite uses the upstream invite contract and persists/updates membership state compatibly instead of synthesizing success without side effects.
    - Test 4: helper-built runtime mounts those live transition hooks under the current constructor path.
  </behavior>
  <action>Port the current Python Team discovery/sync/invite compatibility behavior into Go-owned transition hooks. Expand the Team repository surface only as needed to create/update Team and membership rows compatibly. Then wire these hooks into `newAPITeamServices(...)` so the live API path performs real transition work instead of placeholder local recomputation. Preserve the existing `/api/team*` and shared `/api/ws/task/{task_uuid}` contract.</action>
  <verify>
    <automated>cd backend-go && go test ./internal/team -run 'Test(TeamTransitionExecutor|TeamTransitionGateway|TeamTaskLiveExecution|Accepted|Invite|Discovery|Sync).*' -v && go test ./cmd/api -run 'TestAPITeamRuntime.*' -v</automated>
  </verify>
  <done>The live Team runtime no longer uses placeholder execution for discovery/sync/invite and helper/runtime tests prove the current constructor path mounts real transition hooks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen Team regression coverage for side effects and stabilize auto-launch tests</name>
  <files>backend-go/internal/team/tasks_test.go, backend-go/tests/e2e/payment_team_flow_test.go</files>
  <behavior>
    - Test 1: Team e2e fails if accepted discovery/sync/invite tasks complete without the expected persisted team/membership side effects.
    - Test 2: the discovery reuse test remains deterministic under auto-launch and no longer depends on goroutine timing.
  </behavior>
  <action>Upgrade Team regression coverage so it asserts the real business effect of accepted tasks, not just completed status/logs. Also refactor the flaky discovery reuse test to explicitly control the runtime state it depends on under auto-launch. Keep the assertions tied to the current `static/js/auto_team.js` consumer contract and the persisted `team_*` truth source.</action>
  <verify>
    <automated>cd backend-go && go test ./internal/team -run 'Test(DiscoveryAcceptedTaskReusesOwnerScopeAndWsChannel|TeamTaskLiveExecution|Accepted|Invite|Discovery|Sync).*' -v && go test ./tests/e2e -run 'TestTeamPhaseFourAcceptedTaskLiveFlow' -v</automated>
  </verify>
  <done>Team tests now prove real persisted side effects and remain stable under the auto-launch runtime.</done>
</task>

</tasks>

<verification>
- `cd backend-go && go test ./internal/team -run 'Test(TeamTransitionExecutor|TeamTransitionGateway|TeamTaskLiveExecution|Accepted|Invite|Discovery|Sync).*' -v`
- `cd backend-go && go test ./cmd/api -run 'TestAPITeamRuntime.*' -v`
- `cd backend-go && go test ./tests/e2e -run 'TestTeamPhaseFourAcceptedTaskLiveFlow' -v`
</verification>

<success_criteria>
- Team live runtime no longer returns placeholder success for discovery/sync/invite.
- Team e2e and task tests prove persisted side effects instead of only completed status.
- Shared websocket/task contract remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/04-payment-and-team-domains/04-08-SUMMARY.md`
</output>
