---
phase: 04-payment-and-team-domains
plan: "03"
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - backend-go/cmd/api/main.go
  - backend-go/internal/http/router.go
  - backend-go/internal/http/router_test.go
  - backend-go/tests/e2e/payment_team_flow_test.go
autonomous: false
requirements: [PAY-01, TEAM-01]
must_haves:
  truths:
    - "现有 payment 与 team 页面/静态脚本可以通过 Go API 进程直接访问 Phase 4 迁移后的 `/api/payment*` 与 `/api/team*`。"
    - "Phase 4 完成后，payment/team 成为最后两个被 Go 接管的业务域，而不是继续停留在 router 404 边界之外。"
    - "自动化与人工验证都能证明 payment/team 的 runtime、persistence 和 operator 行为仍与当前页面契约一致。"
  artifacts:
    - "backend-go/cmd/api/main.go 构建并注入 payment/team services。"
    - "backend-go/internal/http/router.go 在现有 `/api/*` 路径上挂载 payment/team handlers，并继续复用现有 `/api/ws/task/{task_uuid}`。"
    - "backend-go/tests/e2e/payment_team_flow_test.go 锁定 payment/team 当前页面 consumer、phase boundary 与关键兼容行为。"
  key_links:
    - "payment/team handlers 通过现有 Go API bootstrap 暴露，不需要前端改路径或改 UI 架构。"
    - "Team accepted-task live flow 继续走现有 `/api/ws/task/{task_uuid}`，payment 则继续保持 bind-card task 的轮询/列表语义。"
    - "Phase 5 cutover 仍未执行；本计划只完成 Go route ownership 与 parity verification，不引入生产切流动作。"
---

<objective>
把 Phase 4 的 payment 和 team slices 接入现有 Go API 进程，并通过自动化与人工验收把 payment/team 从“Phase 4 owner, 未挂载”推进到“Go 已接管、前端可直接消费”。

Purpose: 收口 `PAY-01` 与 `TEAM-01` 的最终 route ownership 和 phase-wide parity 证据，同时明确 Phase 5 cutover 还未开始。
Output: payment/team API bootstrap/router wiring、phase-wide e2e compatibility coverage、人工验证清单。
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-payment-and-team-domains/04-CONTEXT.md
@.planning/phases/04-payment-and-team-domains/04-RESEARCH.md
@.planning/phases/04-payment-and-team-domains/04-UI-SPEC.md
@.planning/phases/04-payment-and-team-domains/04-01-PLAN.md
@.planning/phases/04-payment-and-team-domains/04-02-PLAN.md
@.planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md
@.planning/phases/02-native-registration-runtime/02-04-SUMMARY.md
@.planning/phases/03-management-apis/03-VERIFICATION.md
@backend-go/cmd/api/main.go
@backend-go/internal/http/router.go
@static/js/payment.js
@static/js/accounts.js
@static/js/auto_team.js

<interfaces>
Current bootstrap/router seams:
```go
accountsService := accounts.NewService(accountsRepository)
uploaderService := newAPIUploaderService(uploader.NewPostgresConfigRepository(deps.Postgres), accountsRepository)
return internalhttp.NewRouter(jobService, dependencies...)
```

Current router route ownership boundary before Phase 4:
```go
r.Get("/api/ws/task/{task_uuid}", taskSocketHandler.HandleTaskSocket)
r.Get("/api/ws/batch/{batch_id}", batchSocketHandler.HandleBatchSocket)
// payment/team routes intentionally absent before this phase
```

Phase 4 consumer constraint:
```text
保留当前 Jinja + static/js 页面，不改 `/api/payment*`、`/api/team*` 路径，不引入前端重写或 Phase 5 cutover 动作。
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 Go API bootstrap/router 中挂载 payment 与 team slices，并补 phase-wide e2e compatibility coverage</name>
  <files>backend-go/cmd/api/main.go, backend-go/internal/http/router.go, backend-go/internal/http/router_test.go, backend-go/tests/e2e/payment_team_flow_test.go</files>
  <behavior>
    - Test 1: Go API 进程会构建并注入 payment/team services，并在现有 `/api/payment*`、`/api/team*` 路径上挂载 handlers；现有 `/api/ws/task/{task_uuid}` 仍由 Team accepted-task 复用。
    - Test 2: `static/js/payment.js`、`static/js/accounts.js`、`static/js/auto_team.js` 当前消费的关键 payment/team 路径在 e2e 中保持兼容响应结构和 phase boundary。
    - Test 3: Payment 保持 bind-card task 列表/轮询语义，Team 保持 accepted-task + websocket live flow；不在本计划做 Phase 5 cutover/rollback。
  </behavior>
  <action>按 D-01/D-02/D-04/D-05/D-06/D-07，把 04-01 与 04-02 产出的 payment/team slices 接到 `cmd/api/main.go` 和 `internal/http/router.go`。必须沿用当前 `/api/*` 路径和现有 websocket mounts，不新增专用 router 前缀，也不引入新的前端 harness。e2e 需要直接依据 parity matrix 与现有 `static/js` consumer 写断言，覆盖 payment route ownership、Team accepted-task live flow、以及此前 router 404 边界被 Phase 4 正式接管的事实。</action>
  <verify>
    <automated>cd backend-go && go test ./internal/http -run 'Test(Router|PaymentTeam|PhaseFour).*' -v && go test ./tests/e2e -run 'Test(Payment|Team|PhaseFour).*' -v</automated>
  </verify>
  <done>payment/team 已在 Go API 进程中正式挂载，并有自动化测试证明现有页面 consumer 可以直接打到 Go backend。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验收真实 payment/team 运行时与操作员行为</name>
  <action>基于 Task 1 已完成的 Go API route ownership 和 e2e 契约，进入真实或 staging 环境做最终操作员视角验收；该任务不再新增代码，只负责确认真实 payment/team 运行时与页面行为是否与当前契约一致。</action>
  <what-built>Go-owned payment/team routes、bind-card task runtime、Team accepted-task live flow、phase-wide compatibility e2e coverage</what-built>
  <how-to-verify>
    1. 在真实或 staging 环境打开 `/payment` 页面，验证 `生成支付链接`、`生成并加入绑卡任务（半自动）`、`打开`、`我已完成支付`、`同步订阅`、`删除` 等动作都通过 Go backend 工作，并且 bind-card task 状态按 `link_ready/opened/waiting_user_action/verifying/paid_pending_sync/completed/failed` 演进。
    2. 在 `/accounts` 或 `/payment` 里验证 `session-bootstrap`、`mark-subscription`、`batch-check-subscription` 仍返回当前页面预期的 detail/成功消息，不需要改前端。
    3. 在 `/auto-team` 页面验证 `发现母号`、`批量同步`、Team 列表/详情/memberships、`撤销邀请`、`移除成员`、`绑定本地账号`、`批量邀请` 都走 Go backend；discovery/sync/invite accepted 后，任务中心会通过现有 `/api/ws/task/{task_uuid}` 实时更新并在完成后刷新页面数据。
    4. 确认本阶段没有做生产切流或回滚动作，Phase 5 的 cutover/runbook 仍保持未执行状态。
  </how-to-verify>
  <verify>
    <automated>cd backend-go && go test ./internal/http -run 'Test(Router|PaymentTeam|PhaseFour).*' -v && go test ./tests/e2e -run 'Test(Payment|Team|PhaseFour).*' -v</automated>
  </verify>
  <done>人工确认真实 payment/team 页面与运行时行为符合当前契约，或输出具体缺陷与复现步骤以回流到后续 gap closure。</done>
  <resume-signal>输入 `approved` 继续，或列出发现的问题和复现步骤。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/static-js→Go API router | 现有页面开始直接命中 Go-owned payment/team routes |
| API bootstrap→payment/team slices | 错误 wiring 会导致 routes 仍然 404、依赖漏注入或 phase boundary 漂移 |
| Team accepted-task→shared websocket runtime | Team live flow 依赖现有 `/api/ws/task/{task_uuid}` 的共享控制平面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | S | route ownership boundary | mitigate | router tests 明确锁定 payment/team 已挂载、路径未改名，且仍未触发 Phase 5 cutover 行为。 |
| T-04-03-02 | T | current UI consumer contract | mitigate | e2e 直接依据 parity matrix 和 `static/js/payment.js`/`accounts.js`/`auto_team.js` 的真实消费路径与字段写断言，防止接线后 contract 漂移。 |
| T-04-03-03 | D | Team live task observability | mitigate | 保持 Team accepted-task 复用现有 `/api/ws/task/{task_uuid}`，并在人工验证中显式检查 accepted→running→completed/failed 的可观测链路。 |
</threat_model>

<verification>
- `cd backend-go && go test ./internal/http -run 'Test(Router|PaymentTeam|PhaseFour).*' -v`
- `cd backend-go && go test ./tests/e2e -run 'Test(Payment|Team|PhaseFour).*' -v`
- 人工验证真实 `/payment` 与 `/auto-team` 页面上的关键操作路径
</verification>

<success_criteria>
- payment/team routes 已由 Go API 进程接管，现有页面无需改路径或改 UI 结构。
- 自动化测试证明关键 payment/team consumer contract 与 phase boundary 已闭环。
- 人工验证项明确覆盖真实 payment/team runtime 与 operator 行为，同时不提前执行 Phase 5 cutover。
</success_criteria>

<output>
After completion, create `.planning/phases/04-payment-and-team-domains/04-03-SUMMARY.md`
</output>
