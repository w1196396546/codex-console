---
phase: 04-payment-and-team-domains
plan: "10"
type: execute
wave: 4
depends_on: ["04-08"]
files_modified:
  - backend-go/internal/team/tasks.go
  - backend-go/internal/team/tasks_test.go
  - backend-go/internal/team/transition_executor.go
  - backend-go/internal/team/transition_executor_test.go
  - backend-go/cmd/api/team_runtime_test.go
  - backend-go/tests/e2e/payment_team_flow_test.go
autonomous: true
gap_closure: true
requirements: [TEAM-01]
must_haves:
  truths:
    - "Team sync-batch must execute against every accepted team id, not silently only the first one."
    - "Regression tests fail if a multi-id sync-batch accepted task only mutates the first team."
    - "Mixed-owner discovery scope semantics remain aligned with the Python oracle unless explicitly changed."
  artifacts:
    - "backend-go/internal/team/tasks.go and transition_executor.go preserve multi-id payloads through the live execution path."
    - "backend-go/internal/team/transition_executor_test.go and backend-go/cmd/api/team_runtime_test.go cover multi-id sync-batch semantics."
    - "backend-go/tests/e2e/payment_team_flow_test.go proves batch sync updates every requested team in persisted state."
  key_links:
    - "`sync_all_teams` must not collapse to `Task.TeamID` and ignore `payload.ids`."
    - "Discovery first-owner scope reuse may remain oracle-compatible, but sync-batch must not silently drop later ids."
---

<objective>
Close the remaining Team batch execution gap where `sync-batch` only processes the first requested team id.

Purpose: Final code review found that the current live Team batch path accepts multiple ids but only executes against the first one, which silently drops requested work and leaves later teams stale.
Output: corrected multi-id Team batch execution plus regression coverage that locks the full-id behavior.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-payment-and-team-domains/04-08-SUMMARY.md
@backend-go/internal/team/tasks.go
@backend-go/internal/team/transition_executor.go
@backend-go/internal/team/tasks_test.go
@backend-go/internal/team/transition_executor_test.go
@backend-go/cmd/api/team_runtime_test.go
@backend-go/tests/e2e/payment_team_flow_test.go
@src/web/routes/team.py
@src/services/team/runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve all requested ids through the `sync_all_teams` runtime path</name>
  <files>backend-go/internal/team/tasks.go, backend-go/internal/team/transition_executor.go, backend-go/internal/team/transition_executor_test.go, backend-go/cmd/api/team_runtime_test.go</files>
  <behavior>
    - Test 1: `sync-batch` with multiple team ids executes and persists changes for every requested id.
    - Test 2: single-id sync behavior remains unchanged.
  </behavior>
  <action>Update the Team accepted-task runtime so `sync_all_teams` preserves the full `ids` payload all the way through execution instead of collapsing immediately to the first team id. Keep the accepted-task contract and shared websocket path stable.</action>
  <verify>
    <automated>cd backend-go && go test ./internal/team -run 'Test(TeamTransitionExecutor|Accepted|Sync).*' -v && go test ./cmd/api -run 'TestAPITeamRuntime.*' -v</automated>
  </verify>
  <done>`sync_all_teams` no longer silently ignores later requested team ids.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend integration coverage for multi-id Team batch sync</name>
  <files>backend-go/internal/team/tasks_test.go, backend-go/tests/e2e/payment_team_flow_test.go</files>
  <behavior>
    - Test 1: Team e2e proves multi-id batch sync mutates every requested team.
    - Test 2: discovery scope behavior remains explicitly documented/tested as first-owner keyed, matching the Python oracle.
  </behavior>
  <action>Strengthen tests so the multi-id batch sync gap cannot regress silently. If mixed-owner discovery keeps the current first-owner scope behavior for Python compatibility, make that expectation explicit in tests rather than leaving it ambiguous.</action>
  <verify>
    <automated>cd backend-go && go test ./internal/team -run 'Test(DiscoveryAcceptedTaskReusesOwnerScopeAndWsChannel|Accepted|Sync).*' -v && go test ./tests/e2e -run 'TestTeamPhaseFourAcceptedTaskLiveFlow' -v</automated>
  </verify>
  <done>Multi-id Team batch semantics are covered, and any remaining first-owner discovery behavior is explicit rather than accidental.</done>
</task>

</tasks>

<verification>
- `cd backend-go && go test ./internal/team -run 'Test(TeamTransitionExecutor|Accepted|Sync).*' -v`
- `cd backend-go && go test ./cmd/api -run 'TestAPITeamRuntime.*' -v`
- `cd backend-go && go test ./tests/e2e -run 'TestTeamPhaseFourAcceptedTaskLiveFlow' -v`
</verification>

<success_criteria>
- `sync-batch` processes every requested team id.
- Team batch coverage fails if later ids are silently dropped.
- Discovery first-owner scope behavior is explicit and oracle-aligned.
</success_criteria>

<output>
After completion, create `.planning/phases/04-payment-and-team-domains/04-10-SUMMARY.md`
</output>
