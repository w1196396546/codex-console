---
phase: 03-management-apis
plan: "07"
type: execute
wave: 1
depends_on: ["03-01", "03-06"]
files_modified:
  - backend-go/internal/accounts/service.go
  - backend-go/internal/accounts/service_test.go
  - backend-go/tests/e2e/accounts_flow_test.go
autonomous: true
gap_closure: true
requirements: [MGMT-01, CUT-01]
must_haves:
  truths:
    - "现有 `/accounts-overview` 页面触发 Go `/api/accounts/overview/refresh` 后，成功与失败语义与 Python 端保持一致。"
    - "当刷新后 hourly/weekly quota 仍然都是 `unknown` 时，响应会返回 failed detail，单卡刷新会进入当前页面已有的 warning 分支。"
  artifacts:
    - "backend-go/internal/accounts/service.go 在 `RefreshOverview` 内执行 Python 兼容的远端 quota refresh，而不是把 fallback unknown quota 直接记成 success。"
    - "backend-go/tests/e2e/accounts_flow_test.go 锁定 `/api/accounts/overview/refresh` 的成功/失败 detail 语义。"
  key_links:
    - "`static/js/accounts_overview.js` 继续只通过 `/api/accounts/overview/refresh` 的 `details[].success` 和 `error` 驱动 toast。"
    - "Go accounts slice 写回的 `extra_data.codex_overview` 字段继续兼容现有 overview cards 渲染。"
---

<objective>
关闭 Verification blocker 1：让 `/accounts-overview` 的 quota refresh 结果恢复 Python 兼容语义，而不是把 unknown quota fallback 误判成 success。

Purpose: 恢复 `MGMT-01` 与 `CUT-01` 在 accounts-overview 刷新链路上的真实兼容性，避免当前页面在没有新配额数据时给出成功反馈。
Output: Python-compatible overview refresh 实现与自动化回归测试。
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-management-apis/03-VERIFICATION.md
@.planning/phases/03-management-apis/03-management-apis-01-SUMMARY.md
@.planning/phases/03-management-apis/03-management-apis-06-SUMMARY.md
@src/web/routes/accounts.py
@static/js/accounts_overview.js
@backend-go/internal/accounts/service.go
@backend-go/tests/e2e/accounts_flow_test.go

<interfaces>
From `src/web/routes/accounts.py`:
```python
overview, updated = _get_account_overview_data(
    db,
    account,
    force_refresh=request.force,
    proxy=account_proxy,
    allow_network=True,
)

if overview.get("hourly_quota", {}).get("status") == "unknown" and overview.get("weekly_quota", {}).get("status") == "unknown":
    result["failed_count"] += 1
    result["details"].append({"id": account.id, "email": account.email, "success": False, "error": overview.get("error") or "未获取到配额数据"})
else:
    result["success_count"] += 1
    result["details"].append({"id": account.id, "email": account.email, "success": True, "plan_type": overview.get("plan_type")})
```

From `static/js/accounts_overview.js`:
```javascript
const result = await api.post('/accounts/overview/refresh', { ids: [accountId], force: true, select_all: false });
const currentDetail = details.find((item) => Number(item?.id) === Number(accountId));
if (currentDetail && currentDetail.success === false) {
    toast.warning(`刷新完成但未拿到新配额: ${currentDetail.error || '未知原因'}`, 4500);
} else {
    toast.success(`账号 #${accountId} 配额刷新完成`);
}
```

From `backend-go/internal/accounts/service.go`:
```go
func (s *Service) RefreshOverview(ctx context.Context, req OverviewRefreshRequest) (OverviewRefreshResponse, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 accounts slice 内实现 Python-compatible overview refresh 语义</name>
  <files>backend-go/internal/accounts/service.go, backend-go/internal/accounts/service_test.go</files>
  <behavior>
    - Test 1: 对 Codex 卡片范围内账号执行 refresh 时，会尝试等价于 Python `_get_account_overview_data(... allow_network=True)` 的远端 quota refresh，并把新 overview 写回 `extra_data`.
    - Test 2: 若刷新后 hourly/weekly quota 仍都是 `unknown`，返回 detail `success=false` + `error`，且不增加 `success_count`。
  </behavior>
  <action>按 D-01/D-02/D-06/D-07，仅在 `backend-go/internal/accounts/service.go` 收敛修复 `RefreshOverview`。不要把 payment/team 能力带入此链路，也不要修改前端或新增管理前缀。复用现有 accounts slice 已有的 token/cookie/account extra_data 数据与 HTTP 依赖，补出与 Python `src/web/routes/accounts.py` 一致的远端 overview refresh 逻辑；`fallbackOverview(account)` 只能作为无网络结果的最终投影，不得再被当作 refresh 成功信号。错误详情保持操作员可读的兼容文案，不泄露 token/cookie 原值。</action>
  <verify>
    <automated>cd backend-go && go test ./internal/accounts -run 'Test.*Overview.*Refresh.*' -v</automated>
  </verify>
  <done>`RefreshOverview` 会写回真实刷新后的 overview，并且 unknown quota 情况返回 failed detail 而非 success。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁定 `/api/accounts/overview/refresh` 对当前页面的兼容响应</name>
  <files>backend-go/tests/e2e/accounts_flow_test.go</files>
  <behavior>
    - Test 1: e2e 请求 `/api/accounts/overview/refresh` 时，成功 detail 继续返回 `success=true` 和 `plan_type`。
    - Test 2: unknown quota 路径返回 `success=false` 和 `error`，匹配当前 `static/js/accounts_overview.js` 的 warning 分支预期。
  </behavior>
  <action>在现有 accounts e2e 契约测试上补充 overview refresh 专项断言，直接锁定 blocker 中提到的页面消费语义。不要新增新的 UI harness；沿用 03-06 已建立的“真实 handler + e2e consumer contract”模式，覆盖单卡与批量刷新共享的 detail 结果形状。</action>
  <verify>
    <automated>cd backend-go && go test ./tests/e2e -run 'TestManagementAccountsOverviewRefreshCompatibility' -v</automated>
  </verify>
  <done>accounts e2e 能直接证明 `/accounts-overview` 刷新按钮拿到的 Go 响应已与 Python 兼容。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→`/api/accounts/overview/refresh` | 未受信的账号 ID、筛选条件和 force 标志进入 Go API |
| Go accounts service→远端 quota source | Go 使用账号令牌/会话向外部服务请求最新 overview 数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | T | `backend-go/internal/accounts/service.go` refresh result mapping | mitigate | 仅在拿到非 unknown quota 时计入 success；unknown 路径返回 failed detail，防止状态被错误篡改成成功。 |
| T-03-07-02 | I | refresh error details | mitigate | 错误响应保持兼容文案，不把 access token、session token、cookies 直接暴露到 detail/error。 |
| T-03-07-03 | D | bulk overview refresh loop | accept | 延续当前按选中账号/筛选账号批量刷新语义，不在本 gap 内扩展新的并发模型；已有 select-all/filter 入口控制范围。 |
</threat_model>

<verification>
完成后执行：

- `cd backend-go && go test ./internal/accounts -run 'Test.*Overview.*Refresh.*' -v`
- `cd backend-go && go test ./tests/e2e -run 'TestManagementAccountsOverviewRefreshCompatibility' -v`
</verification>

<success_criteria>
- Go `/api/accounts/overview/refresh` 不再把 fallback unknown quota 计为 success。
- unknown quota 情况会返回 `failed_count` 和 detail `success=false/error=...`。
- 当前 `static/js/accounts_overview.js` 的单卡刷新 warning/success 分支可直接复用，无需前端改动。
</success_criteria>

<output>
After completion, create `.planning/phases/03-management-apis/03-management-apis-07-SUMMARY.md`
</output>
