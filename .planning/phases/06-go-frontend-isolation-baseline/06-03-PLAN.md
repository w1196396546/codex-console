---
phase: 06-go-frontend-isolation-baseline
plan: "03"
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - backend-go/internal/adminui/baseline_test.go
  - backend-go/internal/http/router_test.go
  - backend-go/cmd/api/main_test.go
  - backend-go/tests/e2e/admin_frontend_baseline_test.go
  - tests/test_legacy_webui_smoke.py
autonomous: true
requirements: [ISO-01, ISO-02]
must_haves:
  truths:
    - "自动化证据能同时证明 legacy Python frontend 仍可启动且 Go admin frontend 能独立启动。"
    - "Phase 6 结束时，复制隔离、Go 挂载、legacy fallback 三件事都有可重复执行的回归验证。"
    - "如果基线验证失败，团队会停在 Phase 6 而不是带着不明状态进入 Phase 7 shell 重构。"
  artifacts:
    - "backend-go/tests/e2e/admin_frontend_baseline_test.go 覆盖 Go admin frontend 启动与关键路径 smoke。"
    - "backend-go/internal/adminui/baseline_test.go 覆盖 copied asset manifest 与 source hash 的一致性。"
    - "tests/test_legacy_webui_smoke.py 覆盖 legacy Python frontend 的最小启动/路由烟测。"
  key_links:
    - "Go baseline smoke 直接走 `newAPIHandler(...)` 和 chi router，而不是单独造一套 UI harness。"
    - "legacy smoke 直接从 `src/web/app.py:create_app()` 验证当前 Python frontend 仍可运行。"
    - "Phase 6 验证失败时的回滚点是停止推进新 frontend，而不是改写 legacy frontend 来补救。"
---

<objective>
补齐 Phase 6 的基线验证闭环：证明 legacy Python frontend 未被破坏，且新的 Go admin frontend 已经能独立启动和提供核心路由。

Purpose: 把 `ISO-01` / `ISO-02` 从“看起来已经实现”变成“有自动化证据可反复回归”，为 Phase 7 的 shell 重构提供安全起点。
Output: Go admin smoke tests、copy integrity tests、legacy Python smoke test。
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-go-frontend-isolation-baseline/06-CONTEXT.md
@.planning/phases/06-go-frontend-isolation-baseline/06-01-PLAN.md
@.planning/phases/06-go-frontend-isolation-baseline/06-02-PLAN.md
@src/web/app.py
@backend-go/internal/http/router.go
@backend-go/cmd/api/main.go

<interfaces>
Phase 6 baseline success criteria from roadmap:
```text
1. Go-owned copied frontend directory structure exists.
2. Go runtime has clear admin route/static mount points separated from `/api/*`.
3. Baseline verification proves legacy frontend still runs unchanged and new Go frontend can boot independently.
```

Legacy Python smoke seam:
```python
def create_app() -> FastAPI:
    ...
    app.mount("/static", StaticFiles(...), name="static")
    @app.get("/login")
    @app.get("/")
```

Go smoke seam:
```go
func newAPIHandler(jobService *jobs.Service, dependencies ...any) http.Handler {
	return internalhttp.NewRouter(jobService, dependencies...)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 Go admin frontend 增加启动与路由 smoke coverage</name>
  <files>backend-go/internal/http/router_test.go, backend-go/cmd/api/main_test.go, backend-go/tests/e2e/admin_frontend_baseline_test.go</files>
  <behavior>
    - Test 1: `/admin/login` 返回 200，未认证访问 `/admin/` 会跳转到 `/admin/login?next=/admin/`，`/admin/static/css/style.css` 可由 Go 进程返回。
    - Test 2: `/healthz`、现有 `/api/*` / `/api/ws/*` 关键路径在 admin frontend 加入后仍保持可访问。
    - Test 3: smoke test 直接经过 `newAPIHandler(...)` / `internalhttp.NewRouter(...)` 的真实接线，而不是单独拼假 handler。
  </behavior>
  <action>新增 Go 侧 baseline smoke 测试，锁定 admin frontend 的最小独立启动能力。测试需要覆盖 login page、受保护首页跳转、静态资源返回，以及 admin mount 加入后 healthz 和既有 API/router 不受影响。优先沿用当前 Go e2e 风格：真实 chi router + `httptest.NewServer`，避免再造前端测试框架。</action>
  <verify>
    <automated>cd backend-go && go test ./internal/http -run 'TestRouter.*AdminUI.*' -v && go test ./cmd/api -run 'TestAPI.*AdminUI.*' -v && go test ./tests/e2e -run 'TestAdminFrontendBaseline.*' -v</automated>
  </verify>
  <done>Go admin frontend 的 boot/login/static/path-separation 已有自动化 smoke 证据，不再只是依赖代码阅读判断。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 为 copied asset 与 legacy Python frontend 增加完整性/未破坏验证</name>
  <files>backend-go/internal/adminui/baseline_test.go, tests/test_legacy_webui_smoke.py</files>
  <behavior>
    - Test 1: copied asset baseline test 会逐项验证 `copy_manifest.json` 中记录的 source hash 与 copied target hash，确保 Phase 6 的复制基线仍可追溯。
    - Test 2: legacy Python smoke test 通过 `create_app()` 证明 `/login`、`/`、`/accounts`、`/static` 等关键挂载仍可建立，不需要依赖任何 Go admin frontend 代码。
  </behavior>
  <action>在 Go 侧新增 manifest/baseline 完整性测试，在 Python 侧新增最小 smoke test 证明 legacy frontend 未被破坏。这里不要为了让测试通过去改 `src/web/app.py` 或 legacy 模板；如果 smoke 暴露问题，应把它视为 Phase 6 blocker。该任务的核心目标是“证据化 untouched + independent boot”，而不是继续扩展 UI 功能。</action>
  <verify>
    <automated>cd backend-go && go test ./internal/adminui -run 'TestAdminUIBaseline.*' -v && pytest tests/test_legacy_webui_smoke.py -q</automated>
  </verify>
  <done>Phase 6 已有自动化证据同时证明 copied asset 基线完整、legacy Python frontend 仍可启动，后续 Phase 7 才可以安全开始。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copied asset manifest → verification suite | 若验证不真实，后续 phase 会在错误基线上继续演化 |
| legacy Python frontend → new Go admin frontend milestone | Phase 6 必须证明两条 frontend path 可以并存，而不是彼此破坏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | R | baseline verification | mitigate | Go smoke、manifest 完整性、legacy Python smoke 三类测试分开落地，避免单一测试误报“Phase 6 已完成”。 |
| T-06-03-02 | T | copied asset provenance | mitigate | `baseline_test.go` 直接依据 manifest 校验 source/copy hash，一旦 copied tree 被无证据改动就立即失败。 |
| T-06-03-03 | D | rollout safety | mitigate | 把 Phase 6 验证失败定义为进入 Phase 7 前的阻断条件，发现问题时停止推进新 shell，而不是篡改 legacy frontend 补洞。 |
</threat_model>

<verification>
- `cd backend-go && go test ./internal/http -run 'TestRouter.*AdminUI.*' -v`
- `cd backend-go && go test ./cmd/api -run 'TestAPI.*AdminUI.*' -v`
- `cd backend-go && go test ./tests/e2e -run 'TestAdminFrontendBaseline.*' -v`
- `cd backend-go && go test ./internal/adminui -run 'TestAdminUIBaseline.*' -v`
- `pytest tests/test_legacy_webui_smoke.py -q`
</verification>

<success_criteria>
- 自动化验证能证明 legacy Python frontend 仍然可启动，且 Go admin frontend 能独立提供 login、受保护页面与静态资源。
- copied asset baseline、Go mount baseline、legacy fallback baseline 都有可重复执行的回归命令。
- 如果验证失败，Phase 6 会停在基线修复，而不是直接进入 Phase 7 shell 重构。
</success_criteria>

<risk_and_rollback>
## 风险

- 如果只测 Go 前端不测 legacy fallback，会把“未破坏 legacy”变成口头承诺。
- 如果只测路由 200 不测 manifest/hash，后续 copied assets 漂移很难归因。

## 回滚点

- 本计划主要新增验证资产；若验证暴露问题，回滚点不是删测试继续推进，而是停止进入 Phase 7，回到 06-01/06-02 修正基线。
- 若必须临时回滚，只回滚本计划新增测试文件，不改 legacy frontend 内容。
</risk_and_rollback>

<output>
After completion, create `.planning/phases/06-go-frontend-isolation-baseline/06-03-SUMMARY.md`
</output>
