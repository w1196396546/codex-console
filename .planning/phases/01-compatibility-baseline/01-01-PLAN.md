---
phase: "01-compatibility-baseline"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-compatibility-baseline/01-route-parity-inventory.json
  - .planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md
autonomous: true
requirements:
  - COMP-01
  - COMP-02
user_setup: []
must_haves:
  truths:
    - "Phase 1 has a reviewable matrix naming every remaining Python-owned HTTP and WebSocket surface, plus the current Go baseline routes that are explicitly out of scope for reimplementation."
    - "Current templates and static/js clients are mapped to the exact /api or /api/ws routes they depend on."
    - "Later migration phases can see which domains remain Python-only versus which routes already belong to the Go baseline."
  artifacts:
    - path: ".planning/phases/01-compatibility-baseline/01-route-parity-inventory.json"
      provides: "Machine-readable inventory of Python routes, websocket channels, Go baseline coverage, client consumers, and ownership status."
    - path: ".planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md"
      provides: "Reviewable parity matrix grouped by domain with ownership target, parity scope, and migration blocker notes."
  key_links:
    - from: ".planning/phases/01-compatibility-baseline/01-route-parity-inventory.json"
      to: ".planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md"
      via: "shared route_id, path, method, client_consumers, and ownership_status fields"
      pattern: "route_id|path|method|client_consumers|ownership_status"
---

<objective>
Freeze the remaining route and client compatibility surface before later migration work changes backend ownership.

Purpose: Build the Phase 1 parity baseline around the current Python behavior per D-01/D-02, while treating existing Go registration/jobs/accounts/websocket foundations as already-migrated baseline per D-03/D-04.
Output: A machine-readable route inventory plus a human-reviewable route/client parity matrix for the remaining migration delta.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compatibility-baseline/01-CONTEXT.md
@.planning/phases/01-compatibility-baseline/01-RESEARCH.md
@src/web/routes/__init__.py
@src/web/app.py
@src/web/routes/websocket.py
@backend-go/internal/http/router.go
@backend-go/README.md
@static/js/app.js
@static/js/accounts.js
@static/js/settings.js
@static/js/payment.js
@static/js/auto_team.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 生成剩余 Python 路由与 Go 基线的机器可读清单</name>
  <files>.planning/phases/01-compatibility-baseline/01-route-parity-inventory.json</files>
  <read_first>src/web/routes/__init__.py, src/web/app.py, src/web/routes/accounts.py, src/web/routes/registration.py, src/web/routes/settings.py, src/web/routes/email.py, src/web/routes/payment.py, src/web/routes/logs.py, src/web/routes/team.py, src/web/routes/team_tasks.py, src/web/routes/upload/cpa_services.py, src/web/routes/upload/sub2api_services.py, src/web/routes/upload/tm_services.py, src/web/routes/websocket.py, backend-go/internal/http/router.go, backend-go/README.md</read_first>
  <action>创建 `.planning/phases/01-compatibility-baseline/01-route-parity-inventory.json`，把当前 Python 后端当作兼容性真值源（D-01），为每条仍在迁移范围内的 `/api/*` 与 `/api/ws/*` 路由写入一条记录，并额外纳入当前已存在的 Go 基线路由记录用于排除重规划（D-03/D-04）。JSON 顶层使用 `{"generated_at":"","routes":[...]}`；每条记录必须包含精确字段 `route_id`, `path`, `method`, `transport`, `python_module`, `python_handler`, `client_consumers`, `go_surface`, `ownership_status`, `phase_target`, `parity_scope`, `notes`。`ownership_status` 只能使用 `go-baseline`、`remaining-python`、`shared-compat` 三个值；`parity_scope` 必须显式列出 D-02 要求的 `path`, `method`, `json_fields`, `status_values`, `websocket_semantics`, `critical_side_effects`。把现有 Go jobs、registration/task APIs、websocket streams、account-listing baseline、uploader persistence、native runner foundations 标为 `go-baseline`，并在 `notes` 写明 “Baseline, no Phase 1 reimplementation per D-03/D-04”。不要修改任何 `backend-go` 业务实现文件。</action>
  <verify>python -c "import json, pathlib; p=pathlib.Path('.planning/phases/01-compatibility-baseline/01-route-parity-inventory.json'); data=json.loads(p.read_text(encoding='utf-8')); print(len(data['routes'])); assert any(r['ownership_status']=='remaining-python' for r in data['routes']); assert any(r['ownership_status']=='go-baseline' for r in data['routes'])"</verify>
  <acceptance_criteria>
    - `.planning/phases/01-compatibility-baseline/01-route-parity-inventory.json` contains `"ownership_status": "remaining-python"`
    - `.planning/phases/01-compatibility-baseline/01-route-parity-inventory.json` contains `"ownership_status": "go-baseline"`
    - `.planning/phases/01-compatibility-baseline/01-route-parity-inventory.json` contains `"transport": "websocket"`
    - `.planning/phases/01-compatibility-baseline/01-route-parity-inventory.json` contains `"parity_scope": ["path", "method", "json_fields", "status_values", "websocket_semantics", "critical_side_effects"]`
  </acceptance_criteria>
  <done>JSON inventory exists, parses successfully, and distinguishes the remaining migration delta from the already-migrated Go baseline.</done>
</task>

<task type="auto">
  <name>Task 2: 输出面向后续阶段的路由与客户端 parity matrix</name>
  <files>.planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md</files>
  <read_first>.planning/phases/01-compatibility-baseline/01-route-parity-inventory.json, static/js/app.js, static/js/accounts.js, static/js/settings.js, static/js/payment.js, static/js/auto_team.js, templates/index.html, templates/accounts.html, templates/settings.html, templates/payment.html, templates/auto_team.html</read_first>
  <action>创建 `.planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md`，按 `Registration`, `Accounts`, `Settings and Email Services`, `Upload Service Configs`, `Payment`, `Logs`, `Team and Team Tasks`, `WebSocket Channels` 分节列出 Phase 1 需要冻结的兼容面。每节用 Markdown 表格，列名固定为 `Python route`, `Method`, `Primary clients`, `Go status`, `Ownership target`, `Parity payload/status notes`, `Blocking risk`。必须把模板与 `static/js` 客户端视为一等消费者（D-05），在 `Primary clients` 中写出确切页面和脚本文件名；对当前 Go 基线路由在 `Go status` 中写明 `Baseline, no Phase 1 rework`（D-03/D-04）。矩阵正文开头加 `## Decision Traceability`，列出 D-01 到 D-05 如何落到该文档。不要发明新接口，也不要把 payment/team/management 迁移实现提前到本计划。</action>
  <verify>rg -n "^## Registration$|^## Payment$|^## Team and Team Tasks$|Baseline, no Phase 1 rework|static/js/payment.js|/api/ws/task/\\{task_uuid\\}" .planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md` contains `## Decision Traceability`
    - `.planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md` contains `Baseline, no Phase 1 rework`
    - `.planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md` contains `static/js/payment.js`
    - `.planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md` contains `/api/ws/task/{task_uuid}`
  </acceptance_criteria>
  <done>The markdown matrix is reviewable by domain, names the exact client consumers, and clearly shows which surfaces remain Python-owned versus Go baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python route surface -> parity inventory | Unreviewed omissions here would silently hide migration scope. |
| Current templates/static JS -> parity matrix | Client expectations must be captured as contract inputs, not inferred later from memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | Route parity inventory | mitigate | Read router composition plus every domain route module and websocket module; acceptance criteria require both `remaining-python` and `go-baseline` records so the matrix cannot collapse into one class. |
| T-01-02 | R | Client dependency mapping | mitigate | Force `Primary clients` to name concrete templates and `static/js` files per D-05; verify exact script names and websocket paths with `rg`. |
| T-01-03 | I | Documentation artifact | accept | Route names and client file names are already repository-visible and do not add secret exposure; keep artifacts limited to contract metadata, not live credentials. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -c "import json, pathlib; json.loads(pathlib.Path('.planning/phases/01-compatibility-baseline/01-route-parity-inventory.json').read_text(encoding='utf-8'))"`
- [ ] `rg -n "^## Registration$|^## Payment$|^## Team and Team Tasks$" .planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md`
- [ ] `rg -n "Baseline, no Phase 1 rework|static/js/accounts.js|/api/ws/batch/\\{batch_id\\}" .planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No unrelated backend-go runtime files were modified
- The Phase 1 parity matrix names the remaining Python-owned route surface, the current Go baseline exclusions, and the exact frontend consumers that depend on them
</success_criteria>

<output>
After completion, create `.planning/phases/01-compatibility-baseline/01-01-SUMMARY.md`
</output>
