---
phase: "02-native-registration-runtime"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-go/cmd/worker/main.go
  - backend-go/cmd/worker/main_test.go
  - backend-go/internal/registration/orchestrator.go
  - backend-go/internal/registration/orchestrator_test.go
  - backend-go/internal/registration/proxy_selector.go
  - backend-go/internal/registration/outlook_reservation_store.go
  - backend-go/internal/registration/executor_preparation_test.go
  - backend-go/internal/nativerunner/prepare_signup_flow.go
  - backend-go/internal/nativerunner/prepare_signup_flow_test.go
  - backend-go/internal/nativerunner/auth/login_password.go
  - backend-go/internal/nativerunner/auth/login_password_test.go
  - backend-go/internal/nativerunner/auth/post_signup.go
  - backend-go/internal/nativerunner/auth/post_signup_test.go
  - backend-go/internal/nativerunner/auth/workspace_resolution.go
autonomous: true
requirements:
  - RUN-01
user_setup: []
must_haves:
  truths:
    - "Go worker bootstrap resolves the native registration preparation/runtime dependencies required for normal single, batch-child, and Outlook batch-child execution without relying on the Python runner bridge."
    - "Native signup preparation covers the Python-oracle critical branches for fresh signup, existing-account token completion, workspace continuation, and at least one add-phone or interactive-boundary path."
    - "Phase 2 has executable proof that the normal registration critical path is native before persistence and upload side effects are finalized."
  artifacts:
    - path: "backend-go/cmd/worker/main.go"
      provides: "Worker bootstrap wiring for native registration runner plus preparation dependencies used on the critical path."
    - path: "backend-go/internal/registration/orchestrator.go"
      provides: "Request normalization, service/outlook preparation, proxy selection, and reservation-aware execution planning."
    - path: "backend-go/internal/nativerunner/prepare_signup_flow.go"
      provides: "Native prepare/signup and token-completion critical-path behavior that replaces Python on normal registration execution."
  key_links:
    - from: "backend-go/cmd/worker/main.go"
      to: "backend-go/internal/registration/orchestrator.go"
      via: "newRegistrationPreparationDependencies + registration.WithPreparationDependencies"
      pattern: "newRegistrationPreparationDependencies|WithPreparationDependencies"
    - from: "backend-go/internal/registration/orchestrator.go"
      to: "backend-go/internal/nativerunner/prepare_signup_flow.go"
      via: "RunnerRequest.Plan email_service/proxy/outlook preparation"
      pattern: "Plan.EmailService|Plan.Proxy|Plan.Outlook"
---

<objective>
Close the Go-native registration preparation and execution gaps that still keep Python on the normal registration path.

Purpose: Per D-01/D-02/D-05, extend the existing Go worker/orchestrator/native-runner foundations instead of rewriting them, so normal registration execution no longer depends on the Python runner bridge while Phase 3/4 domains remain untouched per D-06.
Output: Worker/runtime preparation wiring and native critical-path coverage proving Go-owned registration execution is viable for single, batch-child, and Outlook batch-child runs.
</objective>

<execution_context>
@/Users/weihaiqiu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/weihaiqiu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-native-registration-runtime/02-CONTEXT.md
@.planning/phases/02-native-registration-runtime/02-RESEARCH.md
@.planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md
@.planning/phases/01-compatibility-baseline/01-route-client-parity-matrix.md
@backend-go/cmd/worker/main.go
@backend-go/cmd/worker/main_test.go
@backend-go/internal/registration/orchestrator.go
@backend-go/internal/registration/orchestrator_test.go
@backend-go/internal/registration/executor.go
@backend-go/internal/nativerunner/prepare_signup_flow.go
@backend-go/internal/nativerunner/prepare_signup_flow_test.go
@backend-go/internal/nativerunner/auth/login_password.go
@backend-go/internal/nativerunner/auth/workspace_resolution.go
@src/core/register.py
@src/web/routes/registration.py
@docs/superpowers/plans/2026-04-04-go-registration-compat-slice.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补齐 worker 准备依赖与原生执行入口接线</name>
  <files>backend-go/cmd/worker/main.go, backend-go/cmd/worker/main_test.go, backend-go/internal/registration/orchestrator.go, backend-go/internal/registration/orchestrator_test.go, backend-go/internal/registration/proxy_selector.go, backend-go/internal/registration/outlook_reservation_store.go, backend-go/internal/registration/executor_preparation_test.go</files>
  <read_first>.planning/phases/02-native-registration-runtime/02-CONTEXT.md, .planning/phases/02-native-registration-runtime/02-RESEARCH.md, .planning/phases/01-compatibility-baseline/01-runtime-boundary-contract.md, backend-go/cmd/worker/main.go, backend-go/cmd/worker/main_test.go, backend-go/internal/registration/orchestrator.go, backend-go/internal/registration/orchestrator_test.go, backend-go/internal/registration/executor.go, src/web/routes/registration.py</read_first>
  <behavior>
    - Test 1: worker bootstrap injects concrete registration preparation dependencies for settings/email-service/outlook preparation and does not fall back to no-op wiring for normal native execution.
    - Test 2: orchestrator resolves bound service IDs and Outlook reservations deterministically, preserving the current task-bound semantics without introducing a second runtime store.
    - Test 3: executor-preparation coverage proves batch/outlook child jobs receive the prepared request/plan expected by the native runner.
  </behavior>
  <action>按 D-01/D-02/D-05 扩展现有 `backend-go/cmd/worker` 与 `backend-go/internal/registration/orchestrator` 接缝，不要重写 registration shell。把 `newRegistrationPreparationDependencies` 接到 Go 当前可用的 settings/email-services/outlook 读取器，并新增最小的 `proxy_selector.go` 与 `outlook_reservation_store.go` 适配层，让 worker 正常路径可以显式传递代理信息、识别 Outlook 服务占用并为同批并发任务保留唯一 service。保持实现只覆盖注册运行时需要的读取/保留语义，不要引入 Phase 3 的代理管理 API、settings 管理 API、payment 或 team 逻辑（D-06）。先扩充 `main_test.go`、`orchestrator_test.go`、`executor_preparation_test.go` 编码预期，再修改实现；并避免触碰未列出的脏 WIP 文件。</action>
  <verify>cd backend-go && go test ./cmd/worker ./internal/registration -run 'TestNewRegistrationPreparationDependenciesUsesPostgresRepositories|TestOrchestrator.*|TestExecutor.*Preparation.*' -v</verify>
  <acceptance_criteria>
    - `backend-go/cmd/worker/main.go` no longer relies on implicit no-op preparation wiring for normal native registration execution
    - `backend-go/internal/registration/orchestrator.go` includes reservation-aware Outlook preparation and concrete proxy-selection plumbing for the runtime path
    - targeted Go tests cover worker dependency injection and Outlook reservation behavior
  </acceptance_criteria>
  <done>Worker bootstrap and request preparation are wired for the Go-native registration critical path without expanding scope into later-phase management domains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 对齐 native prepare/signup 关键分支与 Python oracle</name>
  <files>backend-go/internal/nativerunner/prepare_signup_flow.go, backend-go/internal/nativerunner/prepare_signup_flow_test.go, backend-go/internal/nativerunner/auth/login_password.go, backend-go/internal/nativerunner/auth/login_password_test.go, backend-go/internal/nativerunner/auth/post_signup.go, backend-go/internal/nativerunner/auth/post_signup_test.go, backend-go/internal/nativerunner/auth/workspace_resolution.go</files>
  <read_first>.planning/phases/02-native-registration-runtime/02-RESEARCH.md, src/core/register.py, backend-go/internal/nativerunner/prepare_signup_flow.go, backend-go/internal/nativerunner/prepare_signup_flow_test.go, backend-go/internal/nativerunner/auth/login_password.go, backend-go/internal/nativerunner/auth/login_password_test.go, backend-go/internal/nativerunner/auth/post_signup.go, backend-go/internal/nativerunner/auth/post_signup_test.go, backend-go/internal/nativerunner/auth/workspace_resolution.go, backend-go/internal/registration/python_runner.go</read_first>
  <behavior>
    - Test 1: fresh-signup native flow completes and emits the persistence payload expected by the Go executor.
    - Test 2: existing-account paths cover passwordless token completion and historical-password login without needing the Python runner bridge on the normal path.
    - Test 3: workspace continuation/fallback behavior matches the Python oracle closely enough that the native path can own the critical execution branch.
    - Test 4: at least one add-phone or interactive-boundary branch is covered explicitly so Phase 2 does not remove the Python bridge while a normal-path healing branch still depends on it.
  </behavior>
  <action>按 D-01/D-02 使用 `src/core/register.py` 作为观察语义真值源，补齐 `prepare_signup_flow.go` 以及必要的 `auth/login_password.go` / `auth/post_signup.go` / `auth/workspace_resolution.go` 分支，使 Go 原生路径覆盖当前 Phase 2 必须接管的关键场景：正常注册、已有账号无 refresh token 的 token completion、历史密码登录补全、workspace 解析兜底，以及至少一个 `add-phone` 或 interactive-boundary 恢复分支。保持 Python runner 仅作为实现期的有界调试辅助，不要把任何正常成功路径重新导回 bridge（D-02）。先在 `prepare_signup_flow_test.go`、`login_password_test.go`、`post_signup_test.go` 中写出失败用例，再以最小改动补实现；不要重构整个 auth 客户端或引入 Phase 3/4 逻辑。</action>
  <verify>cd backend-go && go test ./internal/nativerunner -run 'TestPrepareSignupFlow.*|Test.*Password.*Login.*|Test.*Workspace.*|TestClientContinueCreateAccount.*AddPhone.*|TestAuthPasswordlessTokenCompletionProvider.*AddPhone.*|TestAuthPasswordlessTokenCompletionProvider.*Boundary.*' -v</verify>
  <acceptance_criteria>
    - `backend-go/internal/nativerunner/prepare_signup_flow.go` covers native signup and existing-account completion branches required by Phase 2
    - `backend-go/internal/nativerunner/auth/login_password.go` has regression tests for historical-password continuation semantics
    - targeted native-runner tests include at least one add-phone or interactive-boundary path and prove normal registration execution does not require the Python bridge
  </acceptance_criteria>
  <done>The native runner covers the Phase 2 critical execution branches needed to remove Python from normal registration execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API/worker job payload -> native preparation plan | Untrusted registration input crosses into service selection, proxy resolution, and Outlook reservation logic here. |
| Worker bootstrap -> native runner | Mis-wired dependencies here would silently reintroduce Python bridge dependence on the critical path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | T | worker registration bootstrap | mitigate | Keep wiring in `cmd/worker/main.go` explicit and test-backed so the native runner plus preparation dependencies are the normal path, not the Python bridge. |
| T-02-01-02 | D | Outlook reservation and batch child preparation | mitigate | Add reservation-aware preparation tests and a concrete store adapter so concurrent Outlook tasks do not claim the same service silently. |
| T-02-01-03 | R | native signup/token-completion branch coverage | mitigate | Encode Python-oracle critical branches in `prepare_signup_flow_test.go` and `login_password_test.go` before changing implementation, so parity claims are traceable to executable evidence. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cd backend-go && go test ./cmd/worker ./internal/registration -run 'TestNewRegistrationPreparationDependenciesUsesPostgresRepositories|TestOrchestrator.*|TestExecutor.*Preparation.*' -v`
- [ ] `cd backend-go && go test ./internal/nativerunner -run 'TestPrepareSignupFlow.*|Test.*Password.*Login.*|Test.*Workspace.*' -v`
- [ ] No Python-bridge-only code path is required by the targeted tests for normal registration execution
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Worker/native preparation dependencies are explicit and test-backed
- Normal registration execution coverage no longer depends on the Python runner bridge
- No Phase 3/4 management, payment, or team files were pulled into the change
</success_criteria>

<output>
After completion, create `.planning/phases/02-native-registration-runtime/02-01-SUMMARY.md`
</output>
